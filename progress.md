# 进度日志

## 会话：2026-08-06

### 阶段 1：需求与数据发现

- **状态：** 已完成
- 已完成：确认项目目标、角色边界、Android 支持范围、后端技术栈、登录方式、语音方案、历史记录、模糊搜索和 APK 交付要求。
- 已完成：只读分析两份 Excel 的工作表结构，识别村民与长期车辆数据源及导入风险。
- 已完成：建立数据治理、查询审计、导入发布、并发编辑、运维和量化验收要求。
- 已完成：创建需求、权限、数据、导入、UI、架构、数据库、接口、测试及部署设计文档。

### 阶段 2：需求、交互与架构文档

- **状态：** 已完成
- 已创建：`docs/01-需求规格说明书.md` 至 `docs/10-部署与运维设计.md`。
- 已创建：5 份 ADR 与 `docs/11-Android开发规范.md`，固定后端、搜索、导入、Room 和 Android 架构决策。

### Git 初始化

- 已初始化本地 `main` 分支。
- 已添加 `.gitignore`，排除真实 Excel、密钥、环境变量、构建产物和本机运行数据。
- 校验发现：上游 Skills 引用文件含既有尾随空白；首个提交仅包含本项目文档、计划和忽略规则，不修改第三方内容。
- 提交记录：根提交 `cc5b5ea` 已创建，包含编码前文档、项目 Skills 和规划文件。
- 过程异常：初始仓库不存在 `HEAD`，取消暂存命令未生效，导致 Skills 与文档一并进入根提交；保留该提交，不重写历史，并通过 `.gitattributes` 忽略上游 Skills 的既有尾随空白检查。

### 阶段 3：工程与后端基础

- **状态：** 进行中
- 已创建：独立 Android 与 Ktor Gradle 工程、PostgreSQL 初始化、Docker Compose、环境变量示例和启动测试。
- 已安装：Android API 36 平台，并生成两个 Gradle Wrapper。
- 验证异常：从项目根目录调用子工程 Wrapper 会将根目录误判为 Gradle 构建根；本地命令统一使用 `server/gradlew -p server` 与 `android/gradlew -p android`。

### 开发计划固化

- **状态：** 已完成
- 已将后续开发路线写入 `task_plan.md`，细化为工程基础、权限、Excel 导入、查询服务、普通用户流程、管理员流程和交付九个阶段。
- 每个阶段均明确范围、验收标准、文档回填要求和预期 Git 提交，后续实现以该文件为唯一执行路线来源。

### 阶段 3：数据库迁移与基础服务端能力

- **状态：** 已完成
- 已引入 Flyway、PostgreSQL JDBC 驱动和 Hikari 连接池；API 在具备数据库配置时自动执行版本化迁移。
- 已建立用户、车辆、村民资料、长期车辆资料、导入批次、导入行和审计记录 7 张核心表，以及有效车牌唯一约束、`pg_trgm` 模糊索引、乐观锁字段和更新时间触发器。
- 已建立请求标识、统一错误响应和基础审计写入器；错误响应不输出内部异常内容，日志仅记录请求标识。
- 本地验证：Ktor 自动测试通过；新 Docker Compose 卷中成功执行 1 条迁移，确认 7 张核心表和车牌三元索引存在；API 重启后健康检查继续通过。
- 测试限制：Testcontainers `1.20.6` 与本机 Docker `29.7.1` 的 API 最低版本不兼容，已移除该不兼容测试；以 Docker Compose 全新卷加 SQL 断言替代迁移集成验证。

### 阶段 4：登录、角色与权限

- **状态：** 已完成
- 已新增认证配置入口、JWT 与初始管理员环境变量模板，以及 `refresh_sessions` 的 Flyway V2 迁移。
- 已验证：引入 Ktor JWT、BCrypt 依赖后，服务端 `test` 构建通过。

### 阶段 5：Excel 导入与数据治理

- **状态：** 已完成
- 已实现 Apache POI 工作簿解析，按表头识别五种车辆分类，支持车牌归一化、多车牌拆分和长期车辆单位跨行继承。
- 已实现字段必填、格式警告、数据库长度校验和行级预览；原始值与解析值仅保存在导入批次记录，不输出到服务日志或测试夹具。
- 已实现管理员预览、批次详情、待确认行处置、事务发布和安全回滚接口；发布前更新采用车辆版本检查，回滚采用效果快照并拒绝覆盖后续修改。
- 已新增 Flyway V3、V4 迁移，支持行拆分序号、发布决议、回滚操作者和导入效果快照。
- 本地验证：Kotlin 单元测试通过；隔离 Docker Compose 数据库中，两份真实 Excel 分别完成预览、发布、回滚，五种类别均被识别，回滚后车辆数为零；管理员行处置接口已验证。

### 阶段 6：车辆查询服务

- **状态：** 已完成
- 已建立统一车辆领域类别和车牌归一化工具；导入与查询共同忽略字母大小写、空格、中点、连字符及无效分隔符。
- 已实现已登录用户可用的模糊候选和车辆详情接口。搜索输入归一化后须达到 4 个有效车牌字符，候选最多 20 条且仅返回车牌和车辆所属类型。
- 已实现精确、前缀、包含匹配的稳定排序；详情按车辆 ID 返回村民或长期车辆完整核验资料。
- 已实现详情成功和未命中审计，仅记录规范化车牌、结果和设备类别，不记录身份证号、联系方式或完整设备标识。
- 本地验证：隔离 Docker Compose 中导入两份真实工作簿后，完成大小写搜索、短关键字、未登录、空结果、候选字段、村民与长期车辆详情、审计、20 条上限和连续 20 次查询响应时间断言。

### 阶段 7：Android 客户端与普通用户流程

- **状态：** 已完成
- 已完成：建立 `core`、`domain`、`data` 与 `feature` 分层；通过 Hilt 装配 Retrofit、Room、语音适配器和仓库，使用类型安全 Navigation Compose 在查询页和详情页间只传递车辆标识。
- 已完成：实现车牌归一化、大小写兼容、4 个有效字符门槛、250 毫秒防抖、候选列表、加载、空结果、网络失败、重新查询和会话失效状态。
- 已完成：实现系统语音输入、录音权限拒绝及识别失败回退；识别结果写回可编辑输入框。
- 已完成：实现按账号隔离的 Room 搜索历史，支持查看、删除和清空，且只保存车辆标识、车牌、所属类型与时间。
- 已完成：实现村民和长期车辆详情展示，导航参数不包含完整车辆资料或身份信息。
- 本地验证：8 个 JVM 单元测试通过；`lintDebug` 无错误无警告；调试 APK 构建成功；Compose 与 Room 仪器化测试 APK 编译成功。已连接 Android 12 真机因处于锁屏状态无法完成测试 APK 安装，完整真机执行留待阶段 9 的兼容性验证。

### 阶段 8：管理员维护流程

- **状态：** 已完成
- 已完成：新增管理员车辆、账号、导入批次列表和审计列表 API；车辆创建、读取、更新和逻辑停用均受 JWT 管理员角色保护，车辆和账号更新均使用版本号防止覆盖。
- 已完成：管理员工作台提供概览、车辆、账号、导入和审计标签页；管理员可维护五类车辆字段、创建账号、调整角色、启用或停用账号、选择 Excel、处理待处置导入行、发布与回滚批次。
- 已完成：普通用户不会看到或注册管理导航目的地，服务端普通用户访问 `/admin/*` 返回 HTTP 403；当前登录管理员不可停用或降级自身，系统至少保留一个启用管理员。
- 已完成：补齐服务端校验测试、Android 管理 ViewModel 测试、Compose 管理工作台测试和接口契约；修正根目录 `data/` 忽略规则，确保 Android 网络、Room 与车辆数据层源码可被 Git 追踪。
- 本地验证：服务端自动测试通过；Android JVM 测试、Lint、调试 APK 和仪器化测试 APK 构建通过；独立 Docker Compose 中验证管理员车辆创建更新停用、账号创建启停、导入与审计列表、普通用户越权拒绝和审计写入。

### 阶段 9：测试、性能、构建与交付

- **状态：** 已完成（调试交付）
- 已恢复阶段上下文，完成测试架构、交付约束、真实工作簿位置、Docker 编排、Android 仪器化测试和签名配置的只读核查。
- 已确认：服务端使用 Ktor/Kotlin Test；Android 使用 Compose 与 Room 仪器化测试；端到端验收使用全新 Docker Compose 卷；真实 Excel 仍受 Git 忽略规则保护。
- 已确认：连接的 Android 12 真机 `83bdbca2` 当前锁屏，未执行仪器化断言；必须在设备解锁和测试 APK 安装确认后重跑。
- 已确认：当前未发现 `keystore.properties`、`.jks`、`.keystore` 或 Gradle `signingConfigs`；正式签名 APK 验收取决于未提交的本机发布密钥是否存在。
- 服务端本轮强制重跑 `test` 通过，5 个任务实际执行。
- Android 本轮强制重跑结果：JVM 测试 12 个通过、0 失败、0 跳过；`lintDebug` 报告为“未发现问题”；调试 APK 与仪器化测试 APK 已于 21:22 CST 重新生成。
- 隔离 Docker Compose 验收通过：4 个 Flyway 迁移、9 张业务表、管理员登录、两份真实 Excel 预览、待处置行确认、发布、五类车辆分类、村民与长期车辆详情、两次回滚、大小写模糊查询、四字符门槛、候选最小字段、普通用户越权拒绝、管理员车辆 CRUD、账号启停、审计追溯和数据库最终状态均符合预期。
- 已认证查询连续 30 次的 P95 为 `0.005884` 秒，低于 1 秒目标。
- 验收脚本首次将账号停用状态误写为车辆状态 `INACTIVE`，服务端按契约返回 `400/账号状态无效`；核查后确认 Android 管理端和 API 契约正确使用 `DISABLED`，用正确状态重跑通过，未发现产品代码缺陷。
- 已新增部署运行、管理员操作、用户操作、回滚演练、性能、测试执行和发布说明文档；文档将已通过与真机/签名待办严格区分。
- 真机直接运行已安装测试包失败：设备锁屏阻止 Compose 测试活动创建界面；已安装测试包也不是本轮构建，缺少本轮 Room 测试类。待设备解锁并重新安装本轮 APK 后重跑。
- 发布变体构建成功，但 `apksigner verify` 确认 `app-release-unsigned.apk` 未签名，不能作为正式交付 APK。
- 已销毁 `plateview_phase9_20260806` 的隔离 API、PostgreSQL、网络和数据卷；真实工作簿的导入原始记录没有保留在本机测试容器中。
- Android 12 真机解锁后，重新安装本轮调试 APK 与测试 APK，`connectedDebugAndroidTest` 执行 8 个 Compose 与 Room 测试，全部通过。
- 真机使用仅含虚构车辆的独立 Docker Compose API 和 ADB 端口映射，完成管理员登录、四字符手动模糊查询、详情、历史恢复、系统返回、管理员工作台和普通用户入口隐藏验证。
- 真机断网后显示“无法连接车辆数据服务”和“重新查询”，恢复 API 后候选重新出现。
- 真机冷启动三次为 945、965、942 毫秒，中位数为 945 毫秒；运行期总 PSS 为 141429 KB。
- 真机已实际授予录音权限并进入语音识别入口，退出后手动输入仍可用；本轮未采集到现场口述车牌的识别结果，因此不将语音转写成功记为自动化通过。
- 用户确认本轮使用调试 APK，调试安装依赖 Android 调试签名，不需要发布签名；正式签名 APK 仍在后续分发前由发布密钥构建。
- 已补充语音识别结果回填的 JVM 测试；Android JVM 测试共 13 个通过，0 失败、0 跳过。
- 第九阶段以“调试交付”完成：所有计划内自动化、隔离端到端、Android 12 真机、性能、恢复、文档和回滚验收已通过；正式分发签名作为后续发布前置条件保留。
- 复核发现 ColorOS 未知来源安装确认会使 Gradle 的自动仪器化安装与运行器启动产生竞态；手动确认后以 ADB 安装目标 APK、确认两个包路径并直接启动 Android 测试运行器，8 个测试全部通过。服务端强制重跑 13 个测试、Android JVM 强制重跑 13 个测试与 Lint 均通过。

### 阶段 10：生产服务器部署与正式网络联调

- **状态：** 进行中
- 用户已将 `api.plateview.top` 的 A 记录指向阿里云服务器 `47.96.190.39`，截图显示 Cloudflare 代理已开启。
- 部署目标为 HTTPS API、Ktor、PostgreSQL、Docker Compose 与 Android 正式 API 地址联调；不提交生产 `.env`、密钥、真实 Excel 或数据库数据。
- 服务器已确认可通过 SSH 访问，Docker 与 Compose 可用；现有 `mysql80` 容器不在 PlateView 部署范围内。
- DNS 已解析到 Cloudflare 边缘节点，但公网 HTTP 返回 `521`、HTTPS 尚未握手；等待生产反向代理、证书和阿里云回源端口就绪。
- 服务器尚无交换空间；GitHub HTTPS 拉取结果待专项验证。
- 服务器 GitHub HTTPS 拉取在 20 秒后超时。部署将从本机已推送且待确认的 `main` 提交创建 Git 归档，并通过 SSH 上传，避免部署过程依赖不可用的服务器 GitHub 出站网络。
- 服务器 Docker Registry 访问也超时。Caddy 镜像将由本机拉取、校验并经 SSH 上传，服务器不尝试自行拉取外部镜像。
- 已创建 `/opt/plateview`、2GiB 交换空间、受限权限的生产 `.env`，并通过 SSH 上传 Git 提交 `377dbf7` 的源码归档和三份运行镜像。服务启动后 PostgreSQL 健康、Ktor API 与 Caddy 均运行，Flyway 4 条迁移完成。
- Caddy 为 `api.plateview.top` 获得 Let’s Encrypt 证书；服务器公网地址直连 HTTPS 健康检查返回成功。Cloudflare 代理仍返回 `525`，待用户切换 API 记录为灰云或完成 Cloudflare 源站 TLS 规则排查后再进行公网与真机登录验收。
- 已修复当前 UI 提交的 Compose 编译兼容性：补齐 `sp` 导入、将过时 `containerColor` 替换为 Material 3 容器颜色参数，并改用 `AutoMirrored.KeyboardArrowRight`。正式 API 地址 APK、13 个 JVM 测试与 Lint 已通过；真机 APK 已安装并可启动。
- 用户已把 API 记录切换为灰云。权威 DNS 和服务器均已解析至 `47.96.190.39`，但部分公共递归 DNS 仍缓存旧 Cloudflare 边缘地址；等待传播后重测公网和真机登录。

## 验证记录

| 检查 | 结果 |
| --- | --- |
| 两份 Excel 文件存在 | 通过 |
| Android SDK 平台 31 及以上可用 | 通过 |
| Java 17 可用 | 通过 |
| Docker 可用 | 通过 |
| 当前目录尚无 Android 工程 | 已确认 |

## 重启检查

| 问题 | 当前答案 |
| --- | --- |
| 当前阶段 | 阶段 9：测试、性能、构建与交付 |
| 后续阶段 | 完整测试、性能验证与交付 |
| 最终目标 | 可部署的 PlateView 核验系统与 Android APK |
| 已知事实 | 见 `findings.md` |
| 已完成工作 | 见本日志 |
