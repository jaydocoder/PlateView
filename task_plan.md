# 任务计划：PlateView 车辆核验系统

## 目标

交付支持 Android 12 及以上的 PlateView Android 应用、自建 Kotlin/Ktor/PostgreSQL 后端、Docker Compose 部署包、完整文档及调试与正式签名 APK。授权用户可通过手动或语音模糊查询车牌完成车辆核验；管理员可维护车辆、账号与 Excel 导入数据。

## 执行规则

- 严格按阶段顺序实施；未经本地验证、文档回填和 Git 提交，不进入下一阶段。
- 每个阶段必须更新 `progress.md`、`findings.md`、`.codex/operations-log.md` 和 `.codex/verification-report.md`。
- 每项实现都必须执行相应的本地单元、接口、Compose 或仪器化测试；失败时先修复，不提交缺陷状态。
- Git 提交信息使用简体中文；真实 Excel、`.env`、密钥、构建产物和个人信息不得提交。
- 本计划是唯一的执行路线来源；需求变更先更新对应需求、架构或 ADR 文档，再调整本计划。

## 当前阶段

阶段 9：测试、性能、构建与交付，待开始。

## 阶段 1：需求与数据发现

- [x] 确认普通用户和管理员的角色与功能边界。
- [x] 确认两份 Excel 是首批正式数据源。
- [x] 识别字段结构、跨行继承、多车牌单元格与重复数据风险。
- [x] 明确 Android 12、应用标识、语音方案与交付类型。
- [x] 审核并确认数据字典、分类字典和导入异常规则。

**验收标准：** 需求边界、数据源范围、敏感字段处理方式和关键术语均有书面记录。

**文档：** 需求规格、权限矩阵、数据字典、Excel 导入映射与数据质量规则。

**状态：** 已完成。

## 阶段 2：需求、交互与架构文档

- [x] 编写 PRD、权限矩阵、业务流程与验收标准。
- [x] 编写 UI 规范、页面流程和交互状态。
- [x] 编写系统架构、数据库设计、接口设计和架构决策记录。
- [x] 编写 Android 开发规范、测试计划与部署运维设计。

**验收标准：** 后续开发需要的需求、数据、UI、架构、测试与部署文档均已建立并完成一致性检查。

**文档：** `docs/01-需求规格说明书.md` 至 `docs/11-Android开发规范.md`、`docs/ADR/`。

**状态：** 已完成。

## 阶段 3：工程与后端基础

### 3.1 工程骨架

- [x] 建立 Android、Ktor、PostgreSQL 与 Docker Compose 工程结构。
- [x] 建立 Compose、Material 3、Hilt 与 Ktor 健康检查的最小可运行入口。
- [x] 建立环境变量模板、PostgreSQL 扩展初始化和 Docker 镜像定义。
- [x] 修复 Android Java/Kotlin 17 编译目标不一致问题。
- [x] 完成本地 Android 调试构建、Ktor 健康检查测试与容器健康端点验证。

### 3.2 基础后端能力

- [x] 引入数据库迁移工具并建立迁移执行策略。
- [x] 建立用户、角色、车辆、人员、单位、导入任务与审计记录的数据模型。
- [x] 建立统一错误响应、请求标识和日志脱敏约定。
- [x] 建立基础审计写入能力。

**验收标准：** 空数据库可由 Docker Compose 初始化；Android 调试 APK 与 Ktor 服务可构建；服务健康检查可用；迁移可重复执行。

**文档回填：** 构建命令、环境变量、迁移策略、异常处理约定和验证结果。

**预期 Git 提交：** `建立Android与Ktor工程骨架`、`建立数据库迁移与基础数据模型`。

**状态：** 已完成。

## 阶段 4：登录、角色与权限

- [x] 实现账号密码登录、会话令牌刷新和退出登录。
- [x] 实现普通用户、管理员两类角色及服务端鉴权。
- [x] 实现 Android 登录态保存、退出清理和受保护导航。
- [x] 实现未登录、无权限、令牌失效和网络异常的统一处理。

**验收标准：** 未登录用户不可查询；普通用户不可调用或进入管理员功能；管理员具备管理入口；退出后本机登录态失效。

**文档回填：** 鉴权流程、接口契约、错误码、测试用例和权限矩阵。

**预期 Git 提交：** `实现账号登录与角色权限`。

**状态：** 已完成。

## 阶段 5：Excel 导入与数据治理

- [x] 实现两份 Excel 的解析、字段映射、格式归一化和校验。
- [x] 实现预览、异常行定位、确认发布、回滚和导入任务状态。
- [x] 区分村民车辆、驻景区单位、驻景区企业、干部车辆和喀纳斯旅游发展股份有限公司车辆。
- [x] 记录来源文件、工作表、行号、操作者和发布审计。

**验收标准：** 导入前可预览；异常行可定位；发布后数据可查询；回滚不影响已发布的其他批次。

**文档回填：** 导入规则、异常处理表、字段映射变更和数据质量报告。

**预期 Git 提交：** `实现车辆数据导入与发布`。

**状态：** 已完成。

## 阶段 6：车辆查询服务

- [x] 实现车牌归一化、模糊检索和详情查询接口。
- [x] 归一化后至少四个有效车牌字符才触发查询，忽略字母大小写，返回不超过 20 个候选的车牌与车辆所属类型。
- [x] 固定候选排序：精确匹配、前缀匹配、包含匹配。
- [x] 返回车辆类别、人员、身份证号、单位、通行人员和对应原始业务字段。
- [x] 记录查询审计，禁止身份证号进入日志、搜索历史和测试数据。

**验收标准：** 部分车牌可返回正确候选；候选可进入详情；不同车辆类别展示正确字段；查询接口通过边界与性能测试。

**文档回填：** 查询契约、排序规则、字段脱敏规则、性能结果和接口测试报告。

**预期 Git 提交：** `实现车牌模糊查询与详情服务`。

**状态：** 已完成。

## 阶段 7：Android 客户端与普通用户流程

- [x] 建立 Android 分层、网络客户端、导航、依赖注入和统一界面状态。
- [x] 实现手动输入、250 毫秒防抖、候选列表与车辆详情页。
- [x] 实现系统语音识别，识别结果可编辑，并支持权限拒绝和识别失败回退。
- [x] 实现按账号隔离的本机搜索历史；不保存身份证号、联系方式和车辆详情。
- [x] 实现加载、空结果、网络失败和重试状态。

**验收标准：** 普通用户可完成“登录 -> 手动或语音输入 -> 候选选择 -> 详情核验 -> 历史查看”的完整流程。

**文档回填：** 页面状态、导航关系、语音权限行为、可访问性检查和 Compose 测试结果。

**预期 Git 提交：** `建立Android应用架构与登录流程`、`实现普通用户车辆查询流程`。

**状态：** 已完成。

## 阶段 8：管理员维护流程

- [x] 实现管理员工作台、车辆增删改查和表单校验。
- [x] 实现账号管理、角色调整、账号启停和导入任务管理。
- [x] 实现导入发布、回滚和审计记录查看。
- [x] 实现编辑冲突、无效字段和服务端校验失败的界面反馈。

**验收标准：** 管理员可维护车辆、账号和导入批次；普通用户不可见且不可访问管理员功能；关键操作均可追溯。

**文档回填：** 管理员操作流程、权限验证结果、冲突策略和审计字段说明。

**预期 Git 提交：** `实现管理员车辆与数据管理`。

**状态：** 已完成。

## 阶段 9：测试、性能、构建与交付

- [ ] 执行 Kotlin 单元测试、Ktor 接口测试、数据库迁移测试、导入测试、Compose 测试和仪器化测试。
- [ ] 验证 Android 12及以上的安装、登录、语音输入、查询和详情核验。
- [ ] 执行启动、查询响应、内存、网络失败、权限与数据恢复测试。
- [ ] 构建调试 APK；获得本机正式签名配置后构建正式签名 APK。
- [ ] 完成部署手册、管理员手册、用户手册、发布说明和回滚演练记录。

**验收标准：** 全部阻断级测试通过；Android 12 及以上兼容性验证完成；交付物、验证报告与回滚步骤完整。

**文档回填：** 测试执行报告、性能报告、发布说明、部署记录和回滚演练报告。

**预期 Git 提交：** `完成测试验证与交付准备`。

## 已确认技术决策

| 决策 | 理由 |
| --- | --- |
| Android 使用 Kotlin、Jetpack Compose、Material 3、MVVM、Clean Architecture、Hilt | 满足项目 Android 开发规范并便于测试和维护。 |
| 最低 SDK 为 31 | 满足 Android 12 及以上的目标设备范围。 |
| 后端使用 Kotlin、Ktor、PostgreSQL 与 Docker Compose | 自建后端且前后端语言一致，便于本地与生产部署。 |
| 使用账号密码登录 | 适合内部核验系统，管理员可预先创建和管理账号。 |
| 管理员拥有数据、导入、账号和角色管理权限 | 满足完整后台维护需求。 |
| 普通用户可查看完整身份证号 | 已由需求确认；必须实施查询审计与日志脱敏。 |
| 车辆详情不做离线缓存 | 正式查询始终请求后端最新数据。 |
| 使用 Android 系统语音识别 | 识别结果可编辑，失败时回退手动输入。 |
| 仅本机保存搜索历史 | 历史按账号隔离，不保存身份证号、联系方式或车辆详情。 |
| 正式 APK 使用已有发布密钥 | 密钥和密码仅通过未提交的本机配置提供。 |

## 关键验收指标

- 模糊查询在输入归一化后至少四个有效车牌字符才触发，忽略字母大小写，返回不超过 20 条候选的车牌与车辆所属类型。
- 查询排序固定为：精确匹配、前缀匹配、包含匹配。
- 正常网络环境下，95% 已认证查询请求小于 1 秒。
- 每一条管理员数据、账号和导入发布操作均有审计记录。
- Excel 导入先预览再发布；任何异常行均可定位到源文件、工作表和行号。
- 普通用户不得访问任一管理员接口或页面。
- Android 12及以上完成安装、登录、语音输入、手动查询与详情核验。

## 风险与约束

- Excel 中存在跨行单位、混合多车牌、重复车牌和不规范字段，必须由导入预览与数据管理员处理。
- 身份证号、联系方式和人员信息属于敏感数据，禁止写入应用日志、崩溃上报、搜索历史和测试夹具。
- 未提供生产服务器、域名或发布密钥密码；本轮先交付 Docker Compose 和本地构建能力。
- Android 仪器化测试需要可用的 Android 12 及以上 模拟器或真机；不可用时须记录原因并先补齐验证环境。
