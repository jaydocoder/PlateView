# 普通管理员账号管理恢复验证报告

时间：2026-09-24

## 需求覆盖

- 普通管理员重新显示“账号角色”页签和概览“账号管理”卡片。
- 普通管理员可以进入账号管理、查看账号列表并新增用户。
- 普通管理员继续只能调整非 `admin` 账号的角色和状态。
- 用户名、密码、真实姓名、头像、排班、更新策略、数据访问、远程缓存清理仍由 `admin + ADMIN` 管理。
- 微信同步和数据访问控制仍只对主管理员显示。
- `/admin/dashboard-summary` 对所有管理员返回真实 `userCount`，同时保持轻量接口和按页签延迟加载策略。

## 本地验证

| 检查项 | 结果 |
|---|---|
| 管理工作台 ViewModel 定向测试 | 通过 |
| Android 全量单元测试 | 通过 |
| Android 主代码编译 | 通过 |

## 0.3.31 闪退修复补充验证

时间：2026-09-25 02:54:00 CST

- 根因：目录同步版本校验位于 `runCatching.onSuccess` 回调中，异常未被捕获，导致后台协程杀死应用。
- 修复：车辆、附件、微信车单和聊天目录均使用显式异常处理；版本不一致记录 `SYNC_TARGET_VERSION_MISMATCH` 并进入失败状态，取消异常继续传播。
- `./gradlew :app:testDebugUnitTest`：通过。
- `./gradlew :app:assembleDebug`：通过。
- 真机 `3B65BL021BV00000` 覆盖安装并启动：通过，进程保持存活。
- 启动后 5 秒日志未发现 `FATAL EXCEPTION`、`CatalogConsistency` 崩溃或目录同步未捕获异常。

综合评分：92/100，结论：通过。
| Android Lint | 通过 |
| Debug APK 构建 | 通过 |
| 服务端测试 | 通过 |
| 差异空白检查 | 通过 |

## 权限边界

- `AdminTab.Users` 不再依赖 `isPrimaryAdministrator`。
- `AdminTab.WechatSync` 和 `AdminTab.DataAccess` 仍依赖主管理员身份。
- 服务端用户接口继续要求管理员角色。
- `AdminUserProfilePolicy` 继续阻止普通管理员修改敏感账号资料和 `admin` 账号。
- 普通用户仍不能加载管理工作台数据。

## 审查说明

- 双轴远程审查分别因模型服务 429 和本地代理 503 失败。
- 已以 Git 历史对比、客户端入口调用链、服务端鉴权策略和自动测试完成本地补偿审查。
- 未发现阻断或高风险问题。

## 评分

- 代码质量：96/100
- 测试覆盖：94/100
- 规范遵循：94/100
- 需求匹配：98/100
- 权限边界：97/100
- 综合评分：96/100

结论：通过。
## 2026-09-24 本地微信同步验证

### 需求完整性

- 目标：将本机微信聊天记录、图片和 PDF 定向同步到本地容器。
- 范围：三个已配置微信群，使用独立本地游标。
- 交付物：本地采集器配置、批次幂等修复、容器运行环境和同步数据。
- 回滚：远程配置已保存为 `~/.config/plateview/wechat-collector.env.remote-backup`。

### 自动验证结果

- `server/gradlew test`：通过。
- Docker 镜像构建：通过。
- `/health`：返回 `ok`。
- Flyway：当前数据库版本 V42。
- 采集器 systemd 服务：运行中。
- 消息幂等：首轮完成后下一轮三群新增均为 0。
- 批次状态：1 个同步运行、25 个确认批次、3 个群水位。
- 群状态：三群均为 `HEALTHY`，待处理消息数为 0。
- 附件：419 个原始资源已上传，72 个仅元数据资源保留补传状态。

### 风险与补偿

- 70 张图片和 2 个 PDF 当前本地原文件不可用，无法伪造原文件上传；元数据已保留，采集器会持续补偿。
- 采集器目前持续指向本地 API；恢复远程同步前必须显式恢复备份配置。

### 评分

- 代码质量：94/100
- 测试覆盖：92/100
- 规范遵循：95/100
- 需求匹配：96/100
- 架构一致：95/100
- 风险评估：94/100
- 综合评分：94/100
- 结论：通过。

## 2026-09-24 本地缓存与服务器30秒有界一致性验证

### 需求与交付物

- 目标：前台在线时复用会话心跳，在30秒陈旧窗口内发现并同步车辆、微信车单、聊天和附件目录变化。
- 服务端：V43单行版本、V44变更日志、V45实体创建/当前修订、固定目标分页、90天维护任务。
- Android：应用级一致性协调器、12至18秒随机心跳、Room原子增量、账号隔离、本地优先搜索与详情、新鲜度警示。
- 附件：目录确认与大文件下载解耦，摘要不变不重复下载，撤权删除与下载使用同一文件键互斥。
- 回滚：原始业务数据与旧全量接口保留；协调器失败时继续展示缓存并明确标记未确认。

### 本地自动验证

| 检查项 | 结果 |
|---|---|
| 服务端 `./gradlew test` | 通过 |
| Android 单元测试、Lint、Debug构建 | 通过 |
| `git diff --check` | 通过 |
| OPPO RMX3461 Room仪器测试14项 | 全部通过 |
| Docker最新镜像与健康检查 | 通过 |
| Flyway V43、V44、V45真实迁移 | 通过 |
| 临时空数据库V1至V45共38个迁移 | 全部通过 |
| 账号车辆缓存隔离 | 通过 |
| 账号目录状态隔离与退出清理 | 通过 |
| 固定目标后新增无关实体继续分页 | 返回200 |
| 固定目标内旧实体后续修改 | 返回409且不允许推进版本 |
| 90天日志边界保留与事务回滚 | 通过 |
| 持续约67次 profile 请求/秒 | 1005次、零错误、P95 6.17毫秒 |

### 双轴审查

- 第一轮审查发现固定目标分页、车辆账号隔离、附件粗粒度锁、账号级状态缓存和日志保留问题，均已修复。
- 第二轮审查发现持续写入可能导致同步饥饿、发送者补充字段绕过版本和附件撤权竞态，已通过V45创建修订语义、发送者变更递增版本和文件键互斥修复。
- 本地数据库故障注入证明：无关新增不会中止旧目标；旧实体变化不会被错误标记为旧版本。

### 风险与结论

- Android进入后台或系统冻结时不承诺30秒，页面会保留缓存并显示未确认状态。
- 当前为单实例服务端，内存快照每5秒校准；多实例 `LISTEN/NOTIFY` 仍属于未来部署扩展，不影响当前单实例验收。
- 突发67并发压测P95为887.52毫秒，未发生错误或连接池耗尽，需继续保留监控。

### 评分

- 代码质量：94/100
- 测试覆盖：93/100
- 规范遵循：95/100
- 需求匹配：95/100
- 架构一致：95/100
- 风险评估：92/100
- 综合评分：94/100
- 结论：通过。

## 2026-09-24 附件缓存状态回流修复验证

### 根因结论

- 后台 Worker 已将附件写入磁盘和 Room，但详情页只在用户点击附件后才观察下载任务。
- 页面文件映射没有从 Room 的 `COMPLETED + localPath` 恢复，导致本地文件实际存在时 UI 仍收到 `file = null`。
- 缩略组件把所有 `file = null` 都渲染成无限加载动画，错误地把“尚未回流”“等待原文件”和“正在下载”混成同一状态。

### 修复覆盖

- 微信聊天详情进入后立即观察全部附件任务并恢复已完成本地文件。
- 微信车单详情进入后立即观察全部附件任务并恢复已完成本地文件。
- 管理员微信同步页复用相同 Room 状态，恢复已缓存图片和 PDF。
- 缩略区域未命中本地文件时显示明确后台缓存提示，不再无限转圈。
- 原文件不可用时停止无效下载请求并显示等待采集器补传状态。
- 完整性面板首次加载写入服务端真实统计。

### 本地自动验证

| 检查项 | 结果 |
|---|---|
| 差异空白检查 | 通过 |
| 已完成任务恢复本地附件测试 | 通过 |
| 缺失文件不伪装缓存测试 | 通过 |
| 管理页真实统计首次加载测试 | 通过 |
| Android 全量单元测试（130 项） | 通过 |
| Android Lint | 通过 |
| Debug APK 构建 | 通过 |
| 服务端测试 | 通过 |

### 质量评分

- 代码质量：95/100
- 测试覆盖：94/100
- 规范遵循：95/100
- 需求匹配：97/100
- 架构一致：96/100
- 风险评估：93/100
- 综合评分：95/100
- 结论：通过。

## 2026-09-24 无微信权限目录误同步修复与本地部署验证

### 修复范围

- 无微信访问权限账号不再获得微信车单、聊天记录和附件目录修订号。
- 客户端对旧服务端错误发布的微信目录修订号执行本地撤权兜底。
- 网络失败不会把已经撤权的目录改成离线陈旧状态。
- 首页确认提示只统计账号实际可访问且结果上限大于零的目录。

### 本地自动验证

| 检查项 | 结果 |
|---|---|
| 服务端全量测试 | 通过 |
| Android 全量单元测试 | 通过 |
| Android 静态检查 | 通过 |
| arm64 调试包构建 | 通过 |
| 差异空白检查 | 通过 |
| 本地接口镜像重建与容器替换 | 通过 |
| `/health` 健康检查 | 200 |
| 真机调试包覆盖安装 | 0.3.30 安装成功 |
| `adb reverse tcp:8080 tcp:8080` | 生效 |
| 应用启动与前台进程 | 正常 |
| 精确崩溃日志检查 | 无致命异常 |

### 残余验证边界

- 已确认真机通过本地容器访问 `/auth/profile`，但最终账号级界面效果仍需用户在真机登录无微信权限账号后目视确认。
- USB 断开或手机重启后反向端口映射会失效，需要重新执行反向映射命令。

### 评分

- 代码质量：96/100
- 测试覆盖：95/100
- 规范遵循：96/100
- 需求匹配：97/100
- 架构一致：96/100
- 风险评估：94/100
- 综合评分：96/100
- 结论：通过。

## 2026-09-24 设备级附件共享缓存与0.3.31发布验证

### 需求覆盖

- 退出登录不再删除账号业务缓存和已经校验的图片、PDF 原文件。
- 相同内容的原文件使用 SHA-256 作为设备共享键，不因账号切换重复下载。
- 当前账号必须先出现在服务端授权附件清单中，账号级 Room 任务才会指向共享文件。
- 无微信权限账号没有附件任务、详情数据或共享路径，不能通过界面读取其他账号附件。
- 权限撤销只清理当前账号索引；远程清缓存仍可删除设备共享附件。
- 无摘要文件、下载临时文件和摘要不匹配文件不能跨账号复用。

### 自动验证

| 检查项 | 结果 |
|---|---|
| 跨账号复用同摘要原文件 | 通过 |
| 账号清理保留共享实体 | 通过 |
| 无摘要文件不跨账号共享 | 通过 |
| 摘要不匹配文件拒绝迁移 | 通过 |
| 显式设备清理删除共享文件 | 通过 |
| 退出只清会话并保留缓存 | 通过 |
| 真机仪器测试 | 4项全部通过 |
| Android 全量单元测试 | 通过 |
| Android 静态检查 | 通过 |
| Android 调试 APK 构建 | 通过 |
| 服务端全量测试 | 通过 |

### 性能与风险

- 同摘要原文件在设备上只保留一份，多个账号切换不会产生重复网络下载或重复磁盘占用。
- 旧缓存仅在首次迁移时计算一次摘要；共享文件命中后不重复计算完整文件摘要。
- 无摘要文件无法证明内容一致，保守保留账号隔离并允许重新下载。
- 共享文件保留会增加长期磁盘占用，管理员远程清缓存可执行完整设备清理。

### 评分

- 代码质量：96/100
- 测试覆盖：95/100
- 规范遵循：96/100
- 需求匹配：97/100
- 架构一致：97/100
- 风险评估：95/100
- 综合评分：96/100
- 结论：通过。

## 2026-09-24 退出登录稳定性修复验证

### 根因

- 确认弹窗能稳定触发一次退出回调，按钮本身没有漏点击。
- 原退出实现先执行微信详情、图片和 PDF 缓存清理，最后才清空认证 DataStore。
- 缓存清理需要等待目录同步锁和附件同步锁；后台同步繁忙时会延迟会话删除。
- 缓存清理一旦抛出异常，后续令牌删除不会执行，表现为弹窗关闭但账号仍处于登录状态。

### 修复

- 先读取当前账号编号，再原子清除认证 DataStore。
- 根界面通过会话流立即切换到登录页。
- 随后尽力清理当前账号缓存；失败不再影响退出结果。
- 后台服务地址和 APK 更新地址位于独立配置存储，保持不变。

### 自动验证

| 检查项 | 结果 |
|---|---|
| 会话清除早于缓存清理 | 通过 |
| 缓存清理抛异常仍退出成功 | 通过 |
| 无当前账号时仍可清除会话 | 通过 |
| Android 全量单元测试 | 通过 |
| Android Lint | 通过 |
| Debug APK 构建 | 通过 |
| 差异空白检查 | 通过 |

### 评分

- 代码质量：97/100
- 测试覆盖：96/100
- 规范遵循：96/100
- 需求匹配：98/100
- 架构一致：97/100
- 风险评估：96/100
- 综合评分：97/100
- 结论：通过。

## 2026-09-24 微信车单早八晚十一与一致性最终复核

### 需求覆盖

- 轻型车按“早八晚八”判断：08:00 至 19:59:59显示“不在通行时间”，20:00起显示“通行时间有效”。
- 重型车按“早八晚十一”判断：08:00 至 22:59:59显示“不在通行时间”，23:00起显示“通行时间有效”。
- 14:00 至 16:00:59是轻型车与重型车共享的通行窗口。
- 混合车型车单按当前命中车牌选择对应车型规则；首页候选和详情页复用同一个领域函数。
- “车辆不得停靠三湾”作为限制说明保留，不参与小时有效性计算。
- “早八晚十二”继续按午夜边界处理，不把23:59错误判定为晚十二之后。

### 一致性闭环

- V46已将微信车单和聊天目录版本拆分，避免无关目录同步。
- 权限撤销与策略变化会归零本地版本并重建授权缓存。
- 附件归属变化会刷新关联车单和消息修订。
- 放行发送者配置变化会在事务内重新分类并生成必要增量，不再空推进版本。

### 本地自动验证

| 检查项 | 结果 |
|---|---|
| 通行时间领域定向测试 | 通过 |
| Android 全量单元测试 | 通过 |
| Android Lint | 通过 |
| Debug APK 构建 | 通过 |
| 服务端全量测试 | 通过 |
| `git diff --check` | 通过 |
| 空数据库V1至V46共39个迁移 | 全部通过 |
| 车单与聊天版本独立性及事务回滚 | 通过 |
| OPPO新增Room仪器测试 | 6项全部通过 |
| arm64 Debug APK安装与启动 | 0.3.30安装成功，进程与前台Activity正常 |

### 残余风险

- 当前部署为单实例，使用进程内版本快照和数据库单行版本表；多实例部署前仍需补充 PostgreSQL `LISTEN/NOTIFY`。
- 微信全量重建当前仍以内存暂存后单事务替换为主，超大首次全量数据集需继续观察内存峰值；日常增量不走该路径。
- OPPO首次握手曾收到512字节而协议头预期24字节；重启ADB并恢复Android SDK版本后设备已上线，仪器测试、APK安装和启动验证均已补齐。

### 质量评分

- 代码质量：95/100
- 测试覆盖：93/100
- 规范遵循：96/100
- 需求匹配：97/100
- 架构一致：95/100
- 风险评估：93/100
- 综合评分：95/100
- 结论：通过。
## 2026-09-24 主管理员远程清理用户缓存真机验证

### 验收范围

- 验证主管理员远程清理功能仍然存在且能完成真实设备清理。
- 验证账号附件、设备共享附件、头像和临时缓存均被清除。
- 验证登录会话、客户端实例编号、接口地址与更新地址不会被误删。
- 验证服务端能够从 `SENT` 收敛到 `COMPLETED`，并生成审计记录。
- 验证同一修订号只执行一次，不会持续删除后续新缓存。

### 真机结果

| 检查项 | 结果 |
|---|---|
| 主管理员请求被服务端接受 | 通过，返回 `SENT` |
| 账号附件探针 | 已删除 |
| 设备共享附件探针 | 已删除 |
| 头像探针 | 已删除 |
| 临时缓存探针 | 已删除 |
| 客户端回执 | 通过，已应用修订号为 `1` |
| 管理端汇总状态 | 通过，`COMPLETED`，完成设备 `1/1` |
| 登录状态 | 保留，主界面仍处于前台 |
| 客户端实例编号 | 保持不变 |
| 接口与更新地址 | 保持不变 |
| 同一修订号重复清理 | 未发生，后置探针跨心跳周期仍保留 |
| 审计记录 | 已生成 |
| 测试数据清理 | 已完成 |

### 风险与边界

- 离线设备需要在下次上线并取得新修订号后才会清理，不能把 `SENT` 误认为全部设备已完成。
- 本次验证覆盖单台 OPPO 真机与本地后端；多设备状态汇总由相同的逐设备回执机制处理，但未在本次建立第二台真机验证。
- QA 流程未创建缺陷议题，因为本次未发现功能缺陷。

### 质量评分

- 代码质量：96/100
- 测试覆盖：94/100
- 规范遵循：96/100
- 需求匹配：98/100
- 架构一致：97/100
- 风险评估：94/100
- 综合评分：96/100
- 结论：通过。
# 首页吸顶、微信同步状态与缓存设备分页验证报告

生成时间：2026-09-24 23:39:12 CST

## 一、需求与交付物映射

- 首页全局确认框已删除；车辆候选卡片的“核验就绪/数据未确认”标签保持原判断和颜色。
- 车辆、微信车单、聊天详情顶部确认状态框已删除，后台目录核验和本地优先读取保持不变。
- 车辆、微信车单、微信聊天记录和搜索历史使用原生吸顶标题。
- 登录及 profile 响应携带五态微信同步健康信息；无权限或两类结果上限均为零时返回空状态。
- 管理端显示本机缓存摘要，并按 10、20、50 条分页展示全部客户端，支持前后页与页码跳转。
- 服务端新增成功同步时间和客户端汇总表，明细与汇总在同一事务更新。

## 二、本地验证结果

### 服务端

- `./gradlew test`：通过。
- V1 至 V48 共 41 个迁移在独立临时 PostgreSQL 数据库连续执行：通过。
- V47 旧数据回填：`last_successful_sync_at = last_uploaded_at`，通过。
- 1000 台模拟设备分页查询：单次执行约 1.3 毫秒。
- 5 秒并发压测：11229 次事务，平均延迟约 1.8 毫秒，失败率 0%，超过 100 毫秒请求 0。

### Android

- `:app:testDebugUnitTest`：通过。
- `:app:lintDebug`：通过。
- `:app:assembleDebug`：通过。
- `:app:compileDebugAndroidTestKotlin`：通过。
- OPPO PKM110 安装 arm64 Debug APK：通过。
- 首页确认框、车辆候选标签、五种微信状态、三类详情、管理端本机摘要与分页、异常附件回归等 9 项定向真机仪器测试：全部通过。
- 应用启动后进程存活，日志未发现本应用致命异常。
- `git diff --check`：通过。

## 三、性能与风险评估

- 首页状态来自既有 profile 心跳，没有增加网络请求。
- 缓存设备分页从汇总表读取，1000 台规模下远低于 100 毫秒目标。
- 分页切换只调用缓存状态接口，单元测试确认不会重新加载异常附件。
- 多来源健康采用在线优先；主管理员页仍保留逐来源诊断，未遮蔽单个来源异常。
- 未执行 90 至 108 秒真实停机等待测试；离线判定函数和五态界面已由自动测试覆盖，实际恢复时延仍取决于现有 12 至 18 秒 profile 心跳。
- 未执行 1000 台真实 Android 设备界面并发测试；已使用真实 PostgreSQL 1000 行数据和并发查询压测补偿。

## 四、质量评分

- 代码质量：94/100。复用现有心跳、缓存上报和列表结构，分页职责独立，未新增重复链路。
- 测试覆盖：93/100。覆盖状态聚合、成功心跳条件、分页边界、仓库状态流、ViewModel 请求隔离和真机界面回归。
- 规范遵循：95/100。中文文案与测试、连续迁移、现有构建与测试框架均符合项目约定。
- 需求匹配：96/100。明确保留车辆候选标签，只删除指定确认框，并完成五态显示和本机摘要加分页。
- 架构一致性：94/100。状态复用 profile 心跳，汇总表作为管理查询读模型，明细仍为权威上报数据。
- 风险控制：91/100。迁移、性能、权限和真机启动已验证；真实采集器停机时延采用自动测试与现有心跳推导补偿。

综合评分：94/100

审查建议：通过。

## 六、同步时间具体日期显示复核

复核时间：2026-09-25 00:25:00 CST

- 同步时间不再使用“今天”相对文案，统一显示为 `M月d日 HH:mm`。
- 示例：`最后同步：9月24日 22:20`，同时包含月、日、小时和分钟。
- `:app:testDebugUnitTest`、`assembleDebug`、`assembleDebugAndroidTest`：通过。
- OPPO PKM110 真机 `WorkOrderScreenTest`：19 项全部通过。
- Debug APK 已重新安装到真机。

## 五、微信标题单行状态布局复核

复核时间：2026-09-25 00:12:00 CST

### 需求映射

- 微信车单和微信聊天记录标题改为左侧分类、中间同步状态、右侧数量的单行布局。
- 首页仅显示“微信服务正常”和“微信同步异常”两种状态，服务端原有五态判断不变。
- 正常使用绿色容器，其他状态使用红色容器；两种状态均显示北京时间的最后同步时间。
- 状态和时间字号缩小，标题禁止换行，标题宽度和数量位置保持稳定。

### 本地验证

- `:app:testDebugUnitTest`：通过。
- `:app:compileDebugAndroidTestKotlin`：通过。
- `:app:assembleDebug` 和 `:app:assembleDebugAndroidTest`：通过。
- OPPO PKM110 真机 `WorkOrderScreenTest`：19 项全部通过。
- 回归覆盖五种后端状态、两态文案映射、最后同步时间、车辆候选标签和既有微信车单交互。
- `git diff --check`：通过。

### 质量评分

- 代码质量：95/100
- 测试覆盖：95/100
- 需求匹配：97/100
- 综合评分：95/100
- 审查建议：通过。

## 六、其他长期风险车辆候选权限例外复核

复核时间：2026-09-26 00:30:00 CST

### 需求映射

- 无其他长期车辆权限的账号仍看不到正常状态的其他长期车辆。
- 拉黑、严查的其他长期车辆进入搜索和本地目录候选，仅返回车牌、类别、状态等风险识别信息。
- 受限风险车辆不返回车辆类型、属性、人员档案和单位详情；详情接口仍拒绝访问。
- Android候选卡片保留拉黑、严查状态标签，禁用点击并隐藏详情箭头；点击无导航、无权限提示、无历史写入。
- Room车辆缓存从8迁移到9，保存`detailAccessible`；旧缓存默认可访问，服务端未返回新字段时客户端回退为可访问。
- 风险车辆恢复普通状态时，增量目录返回既有`REVOKE`语义，清除无权限账号本地候选。

### 本地验证

- 服务端`./gradlew test`：通过。
- `VehicleAccessScopeTest`覆盖普通、拉黑、严查候选可见性与详情不可见性，以及目录字段脱敏：通过。
- Android`:app:testDebugUnitTest`：通过，覆盖受限候选不写历史、不导航。
- Android`:app:assembleDebug`、`:app:assembleDebugAndroidTest`：通过。
- `VehicleCacheDatabaseTest`覆盖详情访问标记持久化与Room 8到9迁移默认值：通过编译并纳入真机测试。
- OPPO RMX3461执行定向`:app:connectedDebugAndroidTest`：26项通过，0失败，0跳过。
- `git diff --check`：通过。

### 质量评分

- 代码质量：94/100
- 测试覆盖：93/100
- 需求匹配：97/100
- 风险控制：95/100
- 综合评分：95/100
- 审查建议：通过。

## 七、微信车单三项需求复核与补充

复核时间：2026-09-26

### 实现结论

- 聊天多车牌显示：已实现，首页使用`selectWechatMessageCandidatePlate`按当前搜索词选择命中车牌，未命中才回退第一辆车。
- 同单号动态归并：已实现，短单独存时作为微信车单；同来源、同北京时间年份出现详细结构化记录后，`is_canonical`只保留详细主记录，短记录通过聊天可见条件进入聊天区；主记录删除时数据库触发器选择同组替代记录。
- 年份隔离：已实现，服务端和Room均按`sourceKey/sourceId + orderYear + orderNumber`分组，当前北京时间年份优先，历史年份继续保留。
- 附件复用：已实现，使用`linked_record_id`和`linked_message_id`双归属，不复制文件；本次补充修复了“附件先上传、消息后上传”顺序，消息入库时会反向关联唯一待处理附件。

### 已知边界

- 没有发送者或时间无法唯一定位时，附件保持待处理，不强行绑定错误车单。
- 同一来源、同一年、同一单号的多个短消息在无法唯一判断时不自动覆盖，详细主记录优先。

### 验证

- 服务端`./gradlew test`：通过。
- Android`:app:testDebugUnitTest`：通过。
- Android Room 6到7迁移、同号跨年/跨来源排序和多车牌选择测试已存在并通过此前真机验证。
- 本次附件先到后消息的反向关联代码通过服务端编译和全量测试。
- `git diff --check`：通过。

### 评分

- 需求完整性：96/100
- 数据一致性：95/100
- 测试覆盖：91/100
- 综合评分：94/100
- 结论：通过。
