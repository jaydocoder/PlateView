# GitHub 发行版应用更新验证报告

生成时间：2026-08-09 23:35 CST

## 需求与交付物映射

| 需求 | 交付物 | 验证结果 |
| --- | --- | --- |
| 自动检查新版本 | GitHub Release 数据源、更新 ViewModel、前台检查 | 通过 |
| 更新弹窗 | Material 3 更新弹窗、版本、说明、稍后处理和下载状态 | 通过 |
| 下载与系统安装 | 流式 APK 下载、FileProvider、系统安装 Intent | 通过编译与清单校验 |
| GitHub CLI 指引 | `~/.codex/AGENTS.md` GitHub CLI 工具支持章节 | 通过：已验证登录与版本 |
| 真机验证 | Android 12 真机上的更新弹窗仪器化测试与正式包启动 | 通过 |

## 自动化验证

- Android JVM 测试：24 项通过，包含版本比较、更新发现、下载成功和下载失败状态。
- Android Lint：未发现错误。
- Android 构建：调试 APK、正式 APK、仪器化测试 APK 均成功生成。
- APK 签名：正式 APK 通过 V3 签名校验。
- GitHub 发行版接口：最新标签为 `v0.3.2`，包含 `app-release.apk` 资产。
- 服务端回归：`server/gradlew test` 通过。

## 真机验证

- 设备：RMX3461，Android 12。
- 正式 `0.3.3 / versionCode 6` APK 覆盖安装成功，原有应用数据保留并正常启动。
- 更新弹窗仪器化用例通过：显示新版本与版本号，点击“立即更新”会触发下载事件。
- 测试使用发布证书重签测试 APK；测试结束后已卸载测试包并恢复正式 release APK。
- 当前 GitHub 最新发行版低于已安装的 `0.3.3`，因此应用未显示降级更新弹窗，符合版本比较规则。

## 已知限制

- 系统安装界面必须由用户确认；若设备未允许来自 PlateView 的安装来源，应用会引导至系统设置，不能静默安装。
- 首次获得本更新能力的 `0.3.3` 需要手动安装；后续发布高于 `0.3.3` 的 GitHub 发行版后，应用会在下次前台检查时提示更新。

## 审查结论

| 维度 | 分数 | 结论 |
| --- | ---: | --- |
| 代码质量 | 94/100 | 网络、下载、状态、弹窗和平台安装职责分离。 |
| 测试覆盖 | 93/100 | 覆盖 JVM 状态、构建、清单、签名和 Android 12 真机弹窗交互。 |
| 规范遵循 | 95/100 | 使用 Kotlin、Hilt、StateFlow、Compose Material 3 和 FileProvider。 |
| 需求匹配 | 95/100 | 覆盖 GitHub 发行版检查、用户确认下载与系统安装流程。 |
| 架构一致性 | 94/100 | 复用既有 Retrofit、Hilt、主题与 Activity 平台边界。 |
| 风险评估 | 92/100 | GitHub 网络可达性和系统安装来源授权依赖设备环境。 |

**综合评分：94/100**

**建议：通过。**
