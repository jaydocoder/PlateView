## 编码前检查 - 微信全量搜索与附件

时间：2026-09-20

- 已查阅上下文摘要：`.codex/context-summary-wechat-search-attachments.md`
- 复用服务端 `WorkOrderParser`、`WorkOrderService` 与目录版本机制。
- 复用 Android `WorkOrderRepository`、`GlassSurface` 和 `VehiclePlateBadge`。
- 复用采集器消息指纹、重叠扫描、游标确认与图片衍生图逻辑。
- 遵循现有 Kotlin、Compose、Room、Ktor、Flyway 和 Python 测试风格。
- 已检查工作区车单、车辆查询、管理同步与采集器模块，确认不存在全量聊天搜索或 PDF 通用附件的重复实现。
- `sequential-thinking` 与 `shrimp-task-manager` 不可用，已使用本地检索分析和任务清单替代。

## 编码前检查 - 本地优先匹配与统一附件缓存

时间：2026-09-24

- 已查阅上下文摘要：`.codex/context-summary-管理工作台详情与附件缓存性能.md`。
- 复用 `RoomVehicleCacheRepository` 的车辆目录快照、`RoomWorkOrderRepository` 的车单/聊天目录水位、`findCachedAttachmentFile`、`downloadAttachmentVariant` 和 `attachmentCacheKey`。
- 遵循 Android 的 MVVM、Room、Hilt、WorkManager 与协程测试约定；服务端继续使用 Ktor 路由、JDBC 服务层和目录版本接口。
- 已确认首页搜索原流程仍存在远端搜索测试语义，先迁移测试再改缓存并发，避免把网络调用误认为本地匹配。
- 已确认管理员页面原先进入微信同步页会自动下载全部异常原件，按需求改为点击附件时才提升下载优先级。

## 编码前检查 - 全量授权附件预取

时间：2026-09-24

- 已查阅 `.codex/context-summary-全量授权附件预取.md`。
- 将复用 `WechatAttachmentCacheRepository`、`downloadAttachmentVariant`、Room 下载任务、WorkManager 调度和现有状态上报接口。
- 遵循账号隔离、原文件摘要键、临时文件不可展示、任何已连接网络允许预取的约定。
- 已检查详情页、管理员预览、后台 Worker、服务端清单和缓存统计，确认无需新增第二套缓存或下载框架。

## 编码后声明 - 本地优先匹配与统一附件缓存

时间：2026-09-24

### 已复用组件

- `VehicleCacheRepository.search`：首页车辆候选只查询本地快照。
- `WorkOrderRepository.searchCached/searchMessagesCached`：首页微信车单和聊天记录只查询按账号隔离的 Room 缓存。
- `WechatAttachmentCacheRepository`：统一管理员、详情页和 WorkManager 的磁盘命中、缓存键和按键互斥。
- `downloadAttachmentVariant`：继续负责 Range 续传、SHA-256 校验和原子落盘。

### 行为变化

- 搜索输入阶段不再调用车辆、微信车单或聊天远端搜索接口；后台只做版本检查和增量目录同步。
- 车辆、车单、聊天目录变化后，当前关键词自动重新执行本地匹配。
- 详情页有 Room 详情时立即显示，网络只做静默刷新；图片和附件不再在详情初始化时全部启动下载。
- 管理员微信同步页不再进入即下载全部异常原件，打开具体附件时复用已有缓存或创建单项下载。
- 清理全局 Room 缓存时同时清理所有附件下载任务，避免状态残留。

### 验证

- `android/./gradlew :app:compileDebugKotlin :app:compileDebugUnitTestKotlin`：通过。
- `android/./gradlew :app:testDebugUnitTest`：通过，44 项测试。
- `android/./gradlew :app:compileDebugAndroidTestKotlin`：通过。
- `server/./gradlew test`：通过。
- `git diff --check`：通过。

### 未执行项

- 未连接真机，因此未执行仪器测试和实际网络下载耗时测量。
- 服务端微信同步异常分页和批量候选查询仍属于后续性能改造，不在本次本地优先搜索链路中重复实现。

## 编码后声明 - 附件查看与管理员预览

时间：2026-09-20

- 复用了 `WorkOrderRepository` 的账号隔离文件缓存、现有图片变体接口和 `GlassSurface` 详情面板。
- 服务端新增主管理员附件预览接口，普通用户仍只能访问已关联且具备微信车单权限的附件接口。
- Android 普通微信详情页支持图片预览、PDF 页码切换和原文件请求；车单图片继续沿用原有三级缓存。
- 管理工作台在加载待处理项时预取预览字节并显示附件内容，候选车单仍使用既有手动关联接口。
- 未引入新的图片/PDF依赖，PDF原文件使用系统 `PdfRenderer`，避免服务器实时转码。

## 本地验证记录

时间：2026-09-20

- `server/./gradlew test`：通过。
- `android/./gradlew :app:testDebugUnitTest :app:assembleDebug`：通过。
- `python3 -m unittest collector/test_plateview_wechat_collector.py`：6项通过。
- `git diff --check`：通过。
- 尚未运行Compose仪器测试和真机安装；当前任务未执行生产部署或GitHub推送。

## 真机与本地部署补充验证

时间：2026-09-20

- 修复 V33 在真实历史数据上的迁移失败：旧数据中原始车牌文本最长 230 字符，车辆明细的原始与规范化车牌列调整为 `TEXT`，避免截断事实来源。
- Docker 本地 API 已按项目代理规范重新构建并启动，健康检查返回正常；Flyway V33、V34 均成功执行。
- Android 微信附件下载支持从临时文件长度继续请求，服务端已有 Ktor `PartialContent` 插件提供标准 Range 响应。
- 最新 Debug APK 已安装到真机 RMX3461 并成功启动，启动日志未发现应用崩溃。
- 真机 Compose 仪器测试执行 85 项，82 项通过、3 项失败；失败均为现有界面测试断言无法找到预期中文节点，需要继续校准测试数据和可见区域断言。

## 历史微信图片验证 - 2026-09-21

- 数据库确认图片消息 `local_message_id=1328/1329` 已建立附件记录并精确关联到对应消息。
- 两条附件当前状态为 `METADATA_ONLY`，没有缩略图、预览图或原图路径。
- `wx attachments` 可以读取两条附件的稳定编号；直接执行 `wx extract` 返回“需要 image AES key”，因此本机图片解密密钥尚未就绪。
- 采集器已调整为保留失败图片重试集合，不再因为推进图片游标而永久跳过解密失败的历史图片。
- 未伪造图片内容，也未将不可解密的元数据误标记为可预览。

## 微信聊天记录发送者强调色 - 2026-09-21

- “孙主任”候选名称使用与蓝色号牌一致的深蓝强调色，字号提升为 20sp 并加粗。
- 其他发送者继续使用原有主题色和字号。
- Android 单元测试、Debug 构建、真机安装启动和 `git diff --check` 均通过。

## 增量补齐 - 管理附件处理与PDF交互

时间：2026-09-20

- PDF原文件查看增加双指缩放、拖动，预览规格继续使用采集端首页图，避免错误交给PdfRenderer。
- 管理端增加解除附件关联入口。
- 管理端增加受保护的车单搜索接口，附件待处理项可按单号、车牌或正文关键词搜索候选并点击关联。
- 本地验证：服务端测试、Android单元测试、Debug构建均通过。

## 验收条件

- 任意非空关键词可检索三个指定群中的普通文本消息。
- 车单和普通消息分组且不重复；昵称、群名、正文、单号和车牌均可匹配。
- 结构化车单区域不符优先于时间状态，多车型按车牌分别判定。
- 简短单号可关联前后图片或 PDF；管理员可预览并选择候选记录。
- Android 支持普通消息详情、图片和 PDF 查看，权限失效时清除缓存。
- 服务端、采集器和 Android 本地测试及构建全部通过后才允许提交。

## 发布前真机回归 - 2026-09-21

- 首次完整真机回归执行 85 项，发现 3 项旧测试预期与当前业务规则不一致。
- 状态测试样例地点原为“禾木”，区域校验优先返回“通行区域不符”；调整为“喀纳斯”后再验证时间状态。
- 管理端附件模型已统一支持图片与 PDF，测试断言由“图片记录”同步为“附件记录”。
- 定向重跑管理工作台与车单界面共 29 项，全部通过。
- 完整真机回归共 85 项，全部通过，设备为 RMX3461。
- 本地服务端测试、采集器测试、Android 单元测试、Lint 与 Debug 构建此前均已通过。

## 编码前检查 - 服务器附件目录权限修复

时间：2026-09-21 01:55

- 已查阅 `.codex/context-summary-server-work-order-image-permission.md`。
- 复用 `install-low-pressure-host.sh` 的 root 初始化边界、`plateview-deploy.sh` 的日志与失败保护、部署策略测试的静态断言模式。
- 遵循 Bash `set -Eeuo pipefail`、只输出中文运维日志和失败不切流约定。
- 已检查宿主机初始化、蓝绿部署、GitHub SSH 调度及现有部署测试，确认没有既有附件目录授权逻辑。

## 编码后声明 - 服务器附件目录权限修复

时间：2026-09-21 01:57

- root 宿主机初始化脚本按源码目录所有者创建附件目录，不硬编码实际部署账号。
- 普通部署脚本不提权，目录不可创建或不可写时在部署日志中输出明确中文错误。
- 复用了现有 root 初始化、蓝绿部署失败保护和部署策略测试模式。
- 全部 5 个部署测试、Bash 语法检查、不可写目录故障模拟和 `git diff --check` 均通过。
- 本机未安装 `shellcheck`；已用 Bash 语法检查、行为测试和生产账号写权限验证补偿。
- 七牛云目录已设置为 `750 plateview-deploy:plateview-deploy`，部署账号写权限验证通过。
- 修复提交 `f8c3497` 已推送，服务端部署流水线 `35527507800` 全部通过。
- 生产活动提交、附件挂载、公网健康接口与 Flyway V34 均已核验通过。

## 编码前检查 - 远程微信同步配置

时间：2026-09-21 02:10:00

- 已查阅 `.codex/context-summary-远程微信同步.md`。
- 复用本机 `systemd --user` 采集服务、采集器服务端确认游标和生产蓝绿部署脚本。
- 遵循环境变量配置、秘密值不落日志、候选容器健康后切流的项目约定。
- 已检查采集器配置读取、服务端令牌读取、用户服务和生产部署脚本，确认无需新增代码或部署工具。
- 当前环境没有 `sequential-thinking` 与 `shrimp-task-manager`，已用本地证据检索、任务清单和本文件记录替代。

## 编码后声明 - 远程微信同步配置

时间：2026-09-21 14:57:00

- 生产端 `/opt/plateview/.env` 已配置与本机采集器一致的 64 位令牌，令牌内容未进入命令参数、日志、文档或提交。
- 生产 API 已通过现有蓝绿部署脚本重建，活动提交仍为 `f8c3497540448f8ddd680dbf8676ab7bff311a0c`，公网健康检查正常。
- 本机采集地址已从 `127.0.0.1:8080` 切换为 `https://api.chenxiruyu.dpdns.org`，旧游标已备份后重置并完成全量补传。
- 根因是本机代理链路间歇性发生 TLS 握手超时和 `SSLEOFError`；生产域名现固定解析到服务器公网 IP `38.226.195.208` 并绕过代理直连，同时保留 HTTPS 域名证书校验。
- 采集器增加请求级有限重试：短暂网络错误和服务端 5xx 可重试，鉴权等 4xx 不重试；服务器确认前不推进游标的既有约束保持不变。
- 复用了 `systemd --user` 服务、采集器确认游标、服务端令牌校验和生产蓝绿部署，没有新增运维框架。

## 远程微信同步验证

时间：2026-09-21 14:57:00

- 采集器单元测试：8 项全部通过。
- 直连生产域名 HTTPS 测试：连续 10 次全部返回 200。
- 采集服务已启用且运行中，连续多轮三个群均输出“群同步完成”。
- 生产数据库消息：票务中心工作群 288 条、贾登峪车道口 1447 条、2026车单子接收群 2611 条，共 4346 条。
- 生产数据库附件记录：467 条。
- 三个群生产状态均为 `HEALTHY`，积压均为 0，错误码均为空。

## 编码前检查 - 多车型通行时段判定

时间：2026-09-21 15:35:00

- 已查阅 `.codex/context-summary-多车型通行时段判定.md`。
- 复用 `selectWorkOrderCandidatePlate`、`PlateQueryNormalizer`、`WorkOrder.vehicles` 和现有状态枚举。
- 遵循领域层集中判定、界面仅传递展示对象、北京时间固定测试的项目约定。
- 已对比 `WorkOrderDisplayFormatter.kt`、`SearchScreen.kt`、`WorkOrderDetailScreen.kt` 和对应测试，确认没有现成的按车辆车型筛选备注逻辑。
- 当前环境没有 `sequential-thinking` 与 `shrimp-task-manager`，已用本地代码证据、生产数据和任务清单替代。
- 公网 `/health` 返回正常，活动容器令牌长度校验通过，未输出令牌原文。

## 编码后声明 - 多车型通行时段判定

时间：2026-09-21 15:41:00

- 复用 `selectWorkOrderCandidatePlate` 取得首页或详情当前代表车牌，复用 `PlateQueryNormalizer` 与 `WorkOrder.vehicles` 映射对应车型。
- 通行状态仍由 `WorkOrderDisplayFormatter.kt` 集中计算；搜索页和详情页仅传入当前展示车牌，保持界面层不解析业务备注。
- 多车型备注按轻型、重型分别选择时段；车型未标明“重型”且同时存在两类规则时采用轻型规则。
- “晚十二”按午夜结束边界处理，不再误解释为中午十二点后的放行窗口。
- 对比并沿用 `SearchScreen.kt` 的候选车牌选择、`WorkOrderDetailScreen.kt` 的详情代表车牌和既有北京时间状态测试模式，未新增重复的车牌或日期解析器。
- Android 完整单元测试、Debug APK、测试 APK 构建通过；领域测试中 `WorkOrderDisplayFormatterTest` 共 25 项、0 失败。
- 真机 RMX3461 已安装调试包与测试包；定向 Compose 测试验证 `0920028`、`新H·9078E` 在日间显示“当前不在通行时段”，1 项通过、0 失败。
- 调试应用已在真机成功启动，前台活动为 `com.jaydocoder.plateview.debug/com.jaydocoder.plateview.MainActivity`。
- `git diff --check` 通过，源码中不存在残留的临时调试标记；采集器既有未提交改动未被修改或撤销。

## 本地后端与真机联调启动

时间：2026-09-21 15:45:00

- 检查发现 `plateview-api-1` 与 `plateview-postgres-1` 均已停止约 14 小时，8080 端口没有服务监听。
- 当前环境没有 CC-Panes Runner 管理接口，按 `ccpanes-clean-launch` 的冲突检查原则改用项目原生 Docker Compose 启动流程。
- 使用既有镜像执行 `docker compose --env-file .env up -d --no-build`，避免触发无代理的依赖重建。
- PostgreSQL 已健康，API 已监听本机 8080，电脑端 `/health` 返回 `{"status":"ok"}`。
- Debug APK 的 API 地址为 `http://127.0.0.1:8080/`；已建立 `adb reverse tcp:8080 tcp:8080` 并重启真机 Debug 应用。
- RMX3461 通过自身 `/system/bin/curl` 请求 `http://127.0.0.1:8080/health` 成功，确认手机到本机 Docker 后端的完整链路可用。
- `adb reverse` 依赖当前 USB 调试连接；拔线、关闭 ADB 或手机重启后需要重新建立映射。

## 编码前检查 - 候选精简与重点发送者

时间：2026-09-21 16:21:28 CST

- 已查阅 `.codex/context-summary-候选精简与重点发送者.md`。
- 将复用 `MAXIMUM_SEARCH_RESULT_COUNT`、`RoomVehicleCacheRepository`、`RoomWorkOrderRepository`、`wechat_passage_senders`、`WorkOrderParser.classify`、`WorkOrderPassageState.displayLabel` 和 `WechatSenderName`。
- 遵循服务端集中分类、Flyway 只增迁移、Android MVVM/Room/Retrofit/Compose 分层及中文测试名称约定。
- 已检查车辆、车单、聊天三条搜索链路及发送者配置模块，确认没有现成的统一 8 条常量，也没有徐站和三叔的既有配置。
- 本机微信群成员与服务端消息交叉确认：徐如军稳定账号为 `xurujun9599`，三叔稳定账号为 `wxid_2493514935112`。
- 当前环境缺少强制流程中指定的思考与任务管理接口，已通过本地证据检索、计划清单和操作记录完成等价流程。

## 编码后声明 - 候选精简、状态短文案与重点发送者

时间：2026-09-21 16:46:00 CST

- 复用 `WorkOrderPassageState.displayLabel` 作为首页和微信车单详情的唯一状态文案来源，将“当前不在通行时段”统一缩短为“不在通行时间”。
- 首页候选和详情状态徽标均设置为单行显示，避免窄屏设备把黄色提示拆成两行。
- 复用服务端搜索参数、Room 查询限制和 `SearchViewModel` 合并结果链路，将实时匹配、微信车单、微信聊天记录分别限制为最多 8 条；界面层保留最终截断，防止缓存与网络合并后超量。
- 复用 `wechat_passage_senders` 与 Flyway 增量迁移，新增徐如军（稳定账号 `xurujun9599`，显示“徐站”）和三叔（稳定账号 `wxid_2493514935112`，显示“三叔”），并对符合车牌与放行语义的历史消息重新分类。
- 复用 `WechatSenderName` 的排版结构，为孙主任、徐站、三叔分别使用暖金、冰湖青和沉静蓝强调色，字号统一为 20sp 并加粗，不增加额外背景块。
- 服务端完整测试、Android 单元测试、Android Lint、Debug APK 与测试 APK 构建均通过。
- 本地 Flyway V35 执行成功；徐站和三叔分别有 33 条、46 条历史消息归类为放行消息。
- 真实接口验证三类结果均为 8 条；按“徐站”和“三叔”搜索均返回正确显示称呼。
- OPPO Reno13（设备型号 PKM110）已安装最新调试包与测试包，`WorkOrderScreenTest` 真机整组 11 项全部通过。
- 本地 Docker API 与 PostgreSQL 正常运行，USB 反向映射已建立；`git diff --check` 通过。
- 既有采集器未提交改动保持原样，未撤销、覆盖或纳入本轮功能范围。

## 编码前检查 - 首页分级搜索与 SQL 优化

时间：2026-09-21 17:20:00 CST

- 已查阅 `.codex/context-summary-首页分级搜索与SQL优化.md`。
- 将复用 `PlateQueryNormalizer`、`WorkOrderParser.normalizeSearchText`、`WorkOrderRepository`、`collectLatest`、现有 Retrofit 与 Flyway 机制。
- 遵循 Ktor/JDBC 服务层、Android MVVM/Room/Retrofit/Compose 分层、Flyway 只增迁移和简体中文测试约定。
- 已对比车辆查询、车单查询、微信聊天查询及 Android 首页调度三个现有实现，确认搜索投影、微信聚合接口和批量装载不存在重复实现。
- 现有真实性能基准：生产微信聊天单字查询约 22.8 毫秒、车单 `092` 查询约 18.7 毫秒；公网基础往返约 540 至 630 毫秒。
- 当前环境未提供指定的思考与任务管理接口，已使用源码调用链、生产只读执行计划和任务清单完成等价分析。

## 编码后声明 - 首页分级搜索与 SQL 优化

时间：2026-09-21 18:24:42 CST

- 复用了 `PlateNormalizer`、车辆搜索投影、`WorkOrderParser.normalizeSearchText`、Room 缓存、Retrofit 聚合接口和现有 `collectLatest` 取消链路。
- 首页搜索已固定为“匹配车辆、微信车单、微信聊天记录”，三类各最多 8 条；本地缓存、车辆远程、微信车单本地、微信聊天本地和微信聚合远程均独立启动，远程结果不会被迟到缓存覆盖。
- 服务端精确车牌改为独立 B-tree 查询；车单部分搜索合并车单投影和车辆明细车牌候选；完整车牌车单排序复用业务单号日期排序。
- 微信聚合接口使用监督并发，分别返回车单和聊天分区的失败标记，单侧 SQL 异常不会取消另一侧结果。
- V36 资料触发器在车辆归属变更时同时刷新旧车辆和新车辆；本地事务回归验证两个投影均与函数计算结果一致。
- 服务端测试、Android 单元测试、Android Lint、Debug APK、Android 测试 APK 均通过；`git diff --check` 通过。
- 本地 Docker 镜像按代理规范重建并启动，健康接口返回正常；OPPO Reno13（PKM110）已安装最新包，`WorkOrderScreenTest` 12 项全部通过。
- 真机输入 `092` 后服务端仅收到一次 `/vehicles/search` 和一次 `/work-orders/home-search`，耗时分别为 24 毫秒和 38 毫秒。
- 未提交、未推送、未发布；采集器既有未提交改动保持原样。

## 编码前检查 - 微信图片同步与附件缩放预览

时间：2026-09-21 18:34:00 CST

- 已查阅 `.codex/context-summary-微信图片同步与附件缩放预览.md` 和用户指定的 `wx-cli` 技能全文。
- 将复用 `wx attachments`、`wx extract`、采集器三规格上传、服务端附件变体接口、`PdfRenderer` 和 Compose 双指手势。
- 遵循采集器 JSON 状态原子写入、日志不输出正文、Android MVVM 分层和附件按需下载约定。
- 已对比车单图片查看器、微信消息 PDF 查看器和管理员内嵌预览三个实现，确认应抽取共用媒体查看组件。
- 已用目标群真实附件验证 `wx 0.6.3` 的 V2 解码；样本为 HEVC 原始码流，`ffmpeg -f hevc` 可转换为可显示图片。
- 已确认旧失败队列受查询时间窗口限制，历史附件不会实际重试；本轮将改为持久化完整最小重试记录。
- 当前环境未提供指定的思考与任务管理接口，已用源码调用链、技能文档、真实命令输出和项目计划完成等价流程。

## 编码后声明 - 微信图片同步与附件缩放预览

时间：2026-09-21 19:09:13 CST

- 复用了 `wx attachments` 与 `wx extract` 获取三个指定群的图片附件；针对新版微信返回的裸 HEVC 码流，使用系统现有 `ffmpeg` 转换为高质量 JPEG 后，再沿用既有 Pillow 流程生成 WebP 预览图和缩略图。
- 图片失败重试从仅保存消息编号改为保存最小附件记录、尝试次数和下次执行时间，历史失败项不再受最近查询时间窗口限制；成功后自动清除，退避上限为一小时。
- 复用了服务端既有附件原图、预览图、缩略图上传与下载协议，并为管理员同步异常响应贯通 PDF 页数。
- 抽取 `ZoomableAttachmentViewer` 作为微信消息详情、微信车单详情和管理员待处理附件的统一查看器；图片和 PDF 首页支持双指缩放、拖动、放大、缩小与复位，PDF 原文件使用系统 `PdfRenderer` 分页显示并及时释放页面位图。
- 管理端附件下载改为流式写入账号运行期缓存文件，避免大型 PDF 一次性驻留字节数组；点击待处理缩略图后下载原文件并打开完整查看器。
- 采集器 10 项测试全部通过，服务端完整测试通过，Android 单元测试、静态检查、调试包和测试包构建通过。
- OPPO Reno13（PKM110）已安装最新调试包；图片点击预览与两页 PDF 分页缩放两项专项真机测试全部通过。
- 真机完整 90 项测试首次有两个既有用例因控件位于屏幕外失败；补充符合真实操作的滚动步骤后，两项均已定向重跑通过，业务代码未为测试修改。
- 本地 Docker API 已按代理规范重建并健康运行，USB 反向映射保持 `tcp:8080`。
- 用户级采集服务脚本已更新并重启；图片游标单独重置以回补历史图片，消息与 PDF 游标未变更，回补期间服务保持运行且没有警告日志。
- 三个群图片游标均已推进至最新附件时间，失败重试数均为 0；远程生产数据库最终确认票务中心工作群 61 张、贾登峪车道口 148 张、2026车单子接收群 243 张可用图片，图片类型已无“仅元数据”记录。
- 未提交、未推送、未发布；工作区其他既有改动均保留。

## 编码前检查 - 微信发送者与附件正文展示

时间：2026-09-21 19:20:00 CST

- 已查阅 `.codex/context-summary-微信发送者与附件正文展示.md`。
- 将复用服务端 `readMessage`、Android `WechatSenderName`、详情附件缓存和 `ZoomableAttachmentViewer`。
- 遵循服务端规范字段、Android领域回退、Compose既有玻璃面板和中文测试名称约定。
- 已检查聊天候选、聊天详情、车单详情和附件下载模块，确认不新增重复附件查看能力。
- 当前环境未提供指定的思考与任务管理接口，已用源码调用链、真实截图和任务清单完成等价流程。

## 编码后声明 - 微信发送者与附件正文展示

时间：2026-09-21 19:40:00 CST

- 服务端发送者名称统一按配置称呼、群昵称、原始昵称、稳定微信账号回退，并忽略空字符串；Android领域层提供相同的旧缓存兜底。
- 首页微信聊天候选和聊天详情来源均使用统一发送者解析；车单详情来源同步采用稳定账号兜底，不再出现空发送者行。
- 采集器重复同步文本时会补齐已有消息缺失的发送者字段，不创建重复消息。
- 严格匹配 `[图片] local_id=...` 或 `[文件] ...pdf (...)` 且确有对应附件时，详情不再显示采集占位正文，改为显示图片或 PDF 首页缩略图；带额外用户说明的正文不会被隐藏。
- “微信原始内容”和“相关附件”两个区块均展示缩略图；相关附件保留文件名、页数、可用状态和点击查看原图或 PDF 的入口。
- 修复 PDF 上传漏传 `localMessageId` 的问题，历史 PDF 已重新扫描并与原消息关联；截图对应的 `车辆申请.pdf` 与 `车辆申请（新H63977）.pdf` 均为可用状态且生成首页预览。
- 生产数据复核：贾登峪 7 个可用 PDF、车单群 9 个可用 PDF 全部关联原消息；另有 2 个旧 PDF 因本机原文件缺失保持仅元数据状态，但也已关联消息。
- 采集器 11 项测试、服务端完整测试、Android 单元测试、Lint、调试包和测试包构建通过。
- OPPO Reno13（PKM110）已安装最新调试包，`WorkOrderScreenTest` 15 项全部通过，覆盖首页发送者回退、详情来源、图片占位替换、PDF首页缩略图及附件点击入口。
- 本地 API 已重建并健康运行；采集服务已更新，三个群 PDF 游标均到达最新记录且没有警告。
- 未提交、未推送、未发布。
## 编码前检查 - 附件原件预缓存与多页查看

时间：2026-09-21 22:30:00

- 已查阅上下文摘要：`.codex/context-summary-附件原件预缓存与多页查看.md`。
- 将复用 `RoomWorkOrderRepository.downloadVariant` 的账号隔离持久缓存、`AdminRepository` 的管理员附件接口、`ZoomableAttachmentViewer` 的手势处理和 `evaluateWorkOrderPassageValidity` 的统一时间判定。
- 遵循 Compose 单向状态、视图模型调度下载、仓库负责文件缓存、领域层负责时间规则的项目边界。
- 已对比微信消息详情、微信车单详情、管理员待处理和统一附件查看器四个实现，确认不存在原件主动缓存与本地 PDF 纵向多页浏览的重复实现。
- 当前环境未提供指定的思考、任务管理和 Context7 接口，已用本地源码证据、现有测试和任务计划完成等价流程。

## 编码后声明 - 附件原件预缓存、关联统计与多页查看

时间：2026-09-22 00:18:00 CST

- 微信消息详情、微信车单详情和主管理员“需要处理”均在数据到达后主动下载全部可用附件原件，不再等待点击；原件写入应用持久目录，重复打开直接命中本地文件。
- 图片按原始尺寸解码显示；PDF 卡片从已缓存原 PDF 本地渲染第一页，打开后按原文件真实页数纵向滚动浏览全部页面。
- 查看器保留双指缩放、拖动和复位，删除放大、缩小图标；背景改为主题半透明浅色。
- 管理员页新增“关联文件总数、已完成、待处理”三个服务端真实汇总值；关联、解除关联和暂不关联后立即刷新。
- 附件统计只包含已关联车单或在同群、同发送者、前后两分钟存在候选车单的业务附件，且满足总数等于已完成与待处理之和；普通聊天附件不混入统计。
- 待处理附件接口取消附件异常 100 条截断，全部待关联附件都会进入顺序原件缓存队列，避免并发下载挤占连接。
- 候选关联改为明确的“关联到单号”按钮；搜索框只负责搜索其他候选，不再通过整卡点击产生歧义。
- 微信车单原件加载失败不再永久显示空占位，改为明确的失败提示和重新加载按钮；该逻辑与通行状态无关，适用于“通行时间待核实”等所有车单。
- “早八晚八”按 08:00 前或 20:00 后允许通行，整点和最后有效日边界测试已补齐。
- 服务端完整测试、Android 单元测试、Lint、Debug APK、仪器测试 APK和差异检查均通过。
- USB 真机 PKM110 已安装最终 Debug APK；管理员和车单相关 38 项 Compose 仪器测试全部通过。
- 未提交、未推送、未部署、未发布。
## 编码前检查 - PDF按钮翻页与本地缓存

时间：2026-09-22 00:30:00

- 已查阅上下文摘要：`.codex/context-summary-PDF按钮翻页与本地缓存.md`。
- 复用 `ZoomableAttachmentViewer`、`AttachmentThumbnail`、`RoomWorkOrderRepository.downloadVariant` 和 `NetworkAdminRepository.downloadWechatAttachment`。
- 遵循 Compose 公共组件、ViewModel 主动预取、`filesDir` 持久化缓存和中文测试名称约定。
- 已对比公共查看器、普通附件仓库、管理员附件仓库、微信消息详情和微信车单详情五处实现，确认不需要新增 PDF 缓存层。
- 依赖关系：三个界面入口调用公共查看器；详情 ViewModel 主动请求原件；仓库优先命中本地原文件并在缺失时下载。
- 当前环境未提供指定的深度思考、任务管理、官方文档查询和 GitHub 搜索接口，已用本地代码证据、既有测试和任务清单完成替代流程。

## 编码后声明 - PDF按钮翻页与本地缓存

时间：2026-09-22 00:51:00

- 复用公共 `ZoomableAttachmentViewer`，三个 PDF 入口统一改为一次渲染一页，通过“上一页”和“下一页”按钮切换。
- 页面总数优先读取本地原 PDF 的 `PdfRenderer.pageCount`，服务端页数只作为读取失败时的回退值。
- 保留双指缩放和拖动，切页时按页面标识重置手势状态；放大、缩小和重置图标均已移除。
- 复用普通用户账号隔离缓存与管理员持久化附件缓存；详情 ViewModel 和管理工作台继续主动请求 `original`，缓存命中时不重复访问网络。
- 对比微信消息详情、微信车单详情和管理员待处理预览，三处均通过同一公共组件生效，没有新增重复实现。
- Android 单元测试、Debug APK、测试 APK、Lint 和差异格式检查通过。
- OPPO Reno13（PKM110）真机执行 `AdminWorkspaceScreenTest` 共 23 项，全部通过；两页 PDF 已验证前后翻页及重置按钮不存在。
- 最新 Debug APK 已安装并成功启动，本地 API 健康检查与 USB 8080 反向映射正常。
## 编码前检查 - 发布0.3.29

时间：2026-09-22 01:00:00

- 已查阅 `.codex/context-summary-release-0.3.29.md`。
- 复用 Android APK 工作流、服务端低压力部署工作流、Gradle 本地验证和 GitHub CLI 发布检查。
- 版本号将同时更新 `android/app/build.gradle.kts` 与 `README.md`，`versionCode` 从 32 顺序提升为 33。
- 已对比 0.3.26、0.3.27、0.3.28 三次历史发布，确认继续采用 `main` 提交加 `v0.3.29` 标签的既有模式。
- 当前工作区 Android 与服务端改动属于同一附件显示和管理功能，必须整体提交以保持接口契约一致。
- GitHub 写操作前已执行 `gh auth status`，当前账号为 `jaydocoder`，具备 `repo` 与 `workflow` 权限。
- 本地没有 `android/keystore.properties`，正式签名由 GitHub Actions 仓库密钥完成，本地不创建或记录签名密码。
- 当前环境未提供指定的深度思考、任务管理和官方文档查询接口，已用本地代码证据、历史发行版与任务清单完成替代流程。

## 发布前真机失败与修复 - 0.3.29

时间：2026-09-22 01:10:00

- 首轮 OPPO Reno13 真机回归在 PDF 从第一页切换到第二页时失败，异常为 `Canvas: trying to use a recycled bitmap`。
- 根因是页面状态退出时立即调用 `Bitmap.recycle()`，而 Compose 渲染线程仍可能持有上一帧的图片引用。
- 修复为不在组合状态退出瞬间手动回收位图；查看器仍然一次只保留当前页面状态，失去引用的位图由 Android 运行时安全回收。
- 发布流程已在提交前暂停，失败版本没有提交、推送、打标签或创建发行版。
- 修复后必须重新执行 Android 构建、相关真机整组测试和启动冒烟，全部通过后才恢复发布。

## 提交前验证 - 发布0.3.29

时间：2026-09-22 01:25:00

- 服务端完整测试通过。
- Android 完整单元测试、Lint、Debug APK 和测试 APK 构建通过。
- OPPO Reno13（PKM110）执行管理工作台与车单页面真机回归，最终执行 40 项、0 失败、2 项按当前时间前置条件跳过。
- 本地真实 API 与数据库核对附件统计：总数 114、已完成 113、待处理 1，接口与 SQL 结果一致。
- 最终 Debug APK 清单确认 `versionName=0.3.29`、`versionCode=33`。
- 最终 Debug APK 已安装到 OPPO Reno13 并成功启动，未发现应用崩溃。
- 本地 API 健康检查、微信采集服务和 USB 8080 反向映射均正常。
- 本地缺少发布签名配置，正式签名与发行版资产将由既有 GitHub Actions 仓库密钥生成并验证。

## 发布与微信原件全量重传 - 0.3.29

时间：2026-09-22 14:56:00

- 提交 `bea2955ca63bf153190af35a45979c27ef33fa17` 已推送到 `main`，标签 `v0.3.29` 已创建并推送。
- 主分支服务端部署工作流、Android 校验工作流和标签正式发布工作流均成功。
- GitHub 发行版已生成 `app-release.apk`；下载后确认版本 `0.3.29`、版本代码 33、APK v3 签名有效，SHA-256 为 `f46b42cd3262974ee026a1f9104ff1316349aa3a10cb207cd47b895ec0d248f8`。
- 生产健康检查正常，服务器更新镜像 `latest.json` 已切换为 `v0.3.29`，APK 哈希与 GitHub 资产一致。
- 本机微信采集服务地址已从本地容器切换为 `https://api.chenxiruyu.dpdns.org`，三个群首次远程同步实际新增 10、66、111 条候选，后续幂等扫描为 0。
- 排查发现旧 `wx` 守护进程仍使用未实现 Linux V2 图片密钥的代码；在 `wx-cli-again` 执行 145 项测试和发行构建后，更新本地二进制并重启守护进程。
- 三群 V2 图片抽样解码成功，确认输出原始 JPG 或 HEVC 数据；采集器负责 HEVC 高质量转 JPG。
- 重置图片和 PDF 游标前备份状态文件，只保留聊天消息游标；随后完成 458 张图片和 18 个 PDF 的原件全量重传。
- 重传结束后三个群图片/PDF游标均到达最新记录，图片重试队列均为 0，服务保持运行。
## 编码前检查 - 附件质量补传、首页预取与喀纳斯放行

时间：2026-09-22 16:40:00 CST

- 已查阅 `.codex/context-summary-附件质量补传与首页预取.md`、既有附件预缓存与微信原件重传摘要。
- 将复用 `wx-cli` 资源解析、采集器指数退避、服务端附件幂等更新、账号隔离磁盘缓存、详情失败集合和白名单放行状态函数。
- 遵循 Rust 结构化响应、Python 原子状态、Ktor/JDBC/Flyway、Android MVVM/StateFlow/Compose 和中文测试命名约定。
- 已对比六个相关实现，确认项目没有资源质量字段、附件单调升级和首页候选附件预取的重复能力。
- 当前环境未提供指定的顺序思考、任务管理、官方文档和 GitHub 搜索接口；已通过本地源码、真实附件尺寸与现有测试完成替代流程。
- 用户随后明确范围为缓存服务器中的全部图片和 PDF，而非仅缓存首页候选；实现调整为服务端分页附件目录、Android 启动与登录即时任务、每 15 分钟周期补齐和摘要缓存跳过。

## 本地微信附件全量补传

时间：2026-09-22 23:50:52 CST

- 微信采集服务地址已切换为本地 `http://127.0.0.1:8080`，三个群心跳状态均为 `HEALTHY`。
- 全量补传前备份采集状态文件，重置游标后从 `2000-01-01` 扫描三个群；共发现 460 张图片与 18 条 PDF 文件记录。
- 补传完成后，本地服务保存 381 张原图、79 张微信缩略图、16 个 PDF 原文件；79 张缩略图全部保留重试状态，等待微信下载高清图或原图后自动升级。
- 剩余 2 条 PDF 元数据均对应 `0803021.pdf`，微信本机目录不存在该原文件，因此无法上传文件内容。
- 截图中的张默涵消息对应附件 465，本机只有 4560 字节缩略图；本地附件接口返回 200、`image/webp`、4560 字节，文件可正常读取。
- 本地 API、PostgreSQL 和微信采集服务最终健康检查通过。

## 编码前检查 - 0922022附件加载失败

时间：2026-09-23 00:15:00 CST

- 已查阅上下文摘要：`.codex/context-summary-0922022附件加载失败.md`。
- 复用账号隔离附件目录、`attachmentCacheKey`、SHA-256 校验、范围续传与既有 MockWebServer 测试模式。
- 遵循 Android 数据层负责文件缓存、视图模型负责状态、中文测试名称和 Kotlin 既有格式。
- 已对比 `RoomWorkOrderRepository`、`NetworkAdminRepository`、`ResumableApkDownloader`、`WorkOrderDetailViewModel` 四处实现，确认问题不是服务端原图缺失。
- 真机已稳定复现：附件 478 每次收到 416 后自动全量重试并返回 200，临时文件大小 392207、摘要正确，但未转正。
- 根因定位为 Retrofit 转换响应体读取完成后调用 `response.raw().close()` 抛出异常，阻止临时文件重命名；同时管理员缓存键与详情缓存键分裂造成重复下载。
- 当前环境未提供指定的顺序思考与任务管理接口，已用真实接口、服务端日志、手机缓存和源码逐层验证完成等价流程。

## 编码后声明 - 0922022附件加载失败

时间：2026-09-23 00:25:00 CST

- 修复 Retrofit 转换响应体读取完成后错误关闭 `raw` 响应导致临时文件无法转正的问题；实际响应体现在由 `use` 负责关闭。
- 416 或失败响应在全量重试前主动关闭实际响应体，保留现有断点续传与连接复用行为。
- 微信车单详情、聊天详情与全量附件预取继续共用 `RoomWorkOrderRepository` 下载入口，没有新增重复下载实现。
- 详情在 SHA-256 一致时可复用管理员模块已经写入同账号目录的原件；摘要不一致的旧文件不会被误用。
- 新增真实 Retrofit + MockWebServer 回归测试，修复前稳定失败，修复后完整下载转正、缓存复用和旧缓存拒绝三项均通过。
- Android 全量单元测试、Lint、Debug APK 构建和差异格式检查通过。
- OPPO PKM110 覆盖安装并实测 `0922022`：先验证复用管理员缓存，再移出缓存强制网络下载；接口返回 200，正式 JPG 缓存生成，摘要与服务器一致，详情缩略图与原图查看均正常。
- 未提交、未推送、未发布。
## 编码前检查 - 附件查看与空白缓存修复

时间：2026-09-23 01:30:00

- 已查阅上下文摘要文件：`.codex/context-summary-附件查看与空白缓存修复.md`
- 将复用 `LiquidGlassDialog`、`ZoomableAttachmentViewer`、`downloadAttachmentVariant`、`findCachedAttachmentFile` 和 `attachmentCacheKey`。
- 遵循 Compose、StateFlow、仓库层缓存和账号隔离目录约定。
- 已对比微信车单详情、微信消息详情和管理员待处理附件三套实现。
- 已检查附件缓存模块，确认无需新增另一套下载器。
- 规范要求的顺序思考与任务管理工具当前环境未提供，已使用可执行计划和本地诊断命令完成等价记录。

## 编码后声明 - 附件查看与空白缓存修复

时间：2026-09-23 02:12:00 CST

- 新增共享 `AttachmentViewerDialog`，微信聊天详情、微信车单详情和管理员待处理附件统一复用同一套顶部标题、关闭图标、响应式视口和错误重试布局。
- PDF 视口由固定 `500.dp` 改为依据手机可用高度计算并限制在 `240.dp..420.dp`；上一页、页码和下一页紧贴 PDF 内容，底部独立关闭按钮已删除。
- 管理员附件下载改为复用账号隔离的车单附件目录、摘要校验、断点续传和同附件下载互斥，不再维护另一套仅按附件编号命中的缓存。
- 完整目录同步成功后按服务端现行摘要清理过期文件；网络失败时不清理，避免服务暂时不可用时误删本地附件。
- 空白附件根因已由真实数据和手机缓存共同确认：服务端附件 459 是可解码的 JPEG，但数据库重置复用了编号，旧客户端按编号前缀误选了遗留 PDF。修复后手机只保留当前摘要对应的 JPEG。
- Android 单元测试、Lint、Debug APK、服务端测试全部通过；OPPO PKM110 执行 41 项界面测试，0 失败，2 项按当前时间条件跳过。
- OPPO 真机确认图片原件可显示；三页 PDF 显示页码和翻页按钮，弹窗顶部为文件名与关闭图标，不存在底部关闭按钮。
- 普通用户和普通管理员只要拥有微信车单访问权限，均会在启动、登录及每 15 分钟周期主动缓存全部可用图片和 PDF；全部普通聊天正文离线同步仍未实现，当前仅全量同步车单目录并缓存搜索命中的普通聊天。
- 本轮未提交、未推送、未发布。

## 编码前检查 - 动态访问控制与领导候选

时间：2026-09-23 02:30:00 CST

- 已查阅 `.codex/context-summary-动态访问控制与领导候选.md`。
- 将复用重点发送者识别、`GlassSurface`、主管理员策略、认证资料轮询、Room 缓存清理、WorkManager 同步和多源 APK 下载器。
- 遵循 Ktor/JDBC/Flyway、Android Hilt/MVVM/StateFlow/Compose、DataStore 和中文测试命名约定。
- 已对比首页候选、账号管理、认证会话、车辆缓存、微信附件缓存和应用更新六类实现，确认不存在完整运行时策略与远程清缓存能力。
- 现有工作区包含用户此前的附件功能改动，本任务仅增量修改，不回退任何既有文件。
- 当前环境未提供指定的顺序思考和任务管理专用工具，已通过代码检索、显式任务计划与本地验证记录替代。

## 计划完成度复审 - 动态访问控制与领导候选

时间：2026-09-23

- 按用户提供的完整开发计划逐项核对服务端、Android、Flyway、更新下载和真机验收证据。
- 已确认核心代码路径已覆盖重点发送者样式、主管理员策略、三类搜索上限、远程缓存清理、动态 API 地址和 APK 更新地址。
- 最新 OPPO PKM110 整组仪器测试仍有 1 项失败：`ProfileScreenTest.退出登录显示独立确认文案`；因此不能将本计划标记为全部验收通过。
- 发现计划正文“admin 配置为 0 时回退 10 条”与验收条目“回退 8 条”冲突；当前代码和断言采用 8 条，待业务口径确认后统一。
- 尚未把普通用户、普通管理员、离线、多设备、双域名切换和重启保持配置全部在真机上闭环验证。
- 本次仅完成审查和报告更新，未修改业务代码，未提交、未推送、未部署生产、未发布 APK。

## 独立数据访问控制保存 - 2026-09-23

- 将原先共用一个“确认发布策略”的提交动作拆分为三个独立服务端接口：`/admin/client-policy/limits`、`/admin/client-policy/api-endpoint`、`/admin/client-policy/update-endpoint`。
- 数量保存只更新三类结果上限；后台地址保存只更新业务 API 地址；APK 地址保存只更新更新服务地址，并分别写入审计记录。
- Android 数据访问控制页面新增“保存首页结果数量”和两个“保存并应用”按钮，三个动作互不覆盖未编辑的配置。
- 服务端测试、Android 单元测试、Debug 构建通过；本地 API 容器已重建并健康运行。
- OPPO PKM110 真机执行 98 项仪器测试，0 失败，2 项按当前时间条件跳过；Debug APK 已重新安装并通过启动回归。
- 本轮未提交、未推送、未部署生产、未发布 APK。

## 排班月历循环与模板日期范围 - 2026-09-23

- “我的”和“全部”切换文字改为圆润按钮内垂直、水平居中。
- 月份滚轮改为循环数据源，12月后可继续滑到1月，不会触底停止。
- 排班模板应用弹窗新增开始日期和可选结束日期；未设置结束日期时保持持续循环，设置后仅在开始至结束日期内循环。
- 服务端新增 V39 迁移，为 `schedule_applications` 增加 `effective_until`、日期范围约束和索引；排班查询会在结束日期后停止生成班次。
- 模板列表显示当前应用起始日期和结束日期/循环状态。
- Android 单元测试、Debug 构建通过；服务端测试通过；Debug APK 已安装到 OPPO PKM110 并成功启动；`git diff --check` 通过。
- 本轮未提交、未推送、未部署生产、未发布 APK。

## 清缓存入口调整与软件更新文案 - 2026-09-23

- 移除账号列表卡片上的清缓存垃圾桶按钮，避免在用户列表外直接执行高影响操作。
- 将“清除该账号客户端缓存”按钮放入账号编辑弹窗的“车辆数据访问范围”区域下方，继续复用二次确认、服务端版本指令和设备完成状态。
- 在同一区域显示最近一次清理状态和已完成设备数量，便于管理员确认是否执行完成。
- 将个人页和更新检查弹窗中的“系统更新”统一改为“软件更新”。
- Android 单元测试、Debug 构建通过；Lint 已通过；本地 `/health` 返回 `{"status":"ok"}`。
- Debug APK 已覆盖安装到 OPPO PKM110 并成功启动。
- 本轮未提交、未推送、未部署生产、未发布 APK。

## 数据访问控制按钮仍联动修复 - 2026-09-23

- 根因确认：数据访问控制页面把页面级 `isSaving` 直接传给三个区域，任意保存动作都会让其他按钮共同进入反馈状态。
- 修复为按 `PolicySavingAction` 派生区域级状态：只有当前点击的按钮显示加载指示并禁用，另外两个保存按钮保持独立状态。
- 三个地址区域的连接测试和保存按钮也只响应当前地址区域的保存状态。
- Android 单元测试、Debug 构建、差异检查通过；Debug APK 已重新覆盖安装到 OPPO PKM110 并成功启动。
- 本轮未提交、未推送、未部署生产、未发布 APK。

## 数据访问控制保存反馈与输入框圆角 - 2026-09-23

- 为首页结果数量、后台服务地址和 APK 更新服务地址三个独立保存动作增加独立成功/失败反馈状态。
- 成功后显示对应的中文确认弹窗；会话失效、权限不足和接口异常显示失败弹窗，同时保留原有错误提示条。
- 弹窗不会自动消失，用户点击“知道了”或关闭弹窗后才清除反馈，避免保存结果被错过。
- 三个数量输入框和两个地址输入框统一使用 `cornerExtraLarge` 圆角，保持数据访问控制页面与液态玻璃风格一致。
- 任一策略保存动作执行时，三个保存按钮和连接测试按钮均明确禁用，避免误判为多个动作同时执行。
- `testDebugUnitTest assembleDebug lintDebug` 全部通过；Lint 无错误。
- Debug APK 覆盖安装到 OPPO PKM110（设备序列号 `3B65BL021BV00000`）成功，应用启动冒烟检查通过。
- 本轮未提交、未推送、未部署生产、未发布 APK。

## 车辆结果数量设置为10时远程加载失败修复 - 2026-09-23

- 根因：V38 策略允许车辆结果数量为 `0..50`，但车辆查询常量 `MAXIMUM_SEARCH_RESULT_COUNT` 仍为 8；管理员保存 10 后，远程车辆查询触发 `require(limit in 1..8)`，客户端本地缓存仍能显示旧结果但远程分区显示加载失败。
- 修复：将车辆查询最大数量统一为 50，并同步更新对应单元测试；默认策略仍为 8，只有管理员配置后才返回 10 或其他有效值。
- 服务端测试通过；Docker API 镜像按代理规范重建并启动，`/health` 返回正常。
- 本轮未提交、未推送、未部署生产、未发布 APK。

## 数据访问控制按钮状态联动修复 - 2026-09-23

- 根因：三个独立保存接口虽然已经拆开，但界面仍共用页面级 `isSaving`，任一保存动作都会让三个按钮同时进入禁用/保存态。
- 修复：新增 `PolicySavingAction`，分别记录数量、后台地址和 APK 地址保存动作；按钮根据当前动作独立计算启用状态。
- Android 单元测试和 Debug 构建通过；最新 Debug APK 已安装到 OPPO PKM110，应用启动正常。
- 本轮未提交、未推送、未部署生产、未发布 APK。
## 远程微信数据清空与全量重同步 - 2026-09-23

- 用户已明确确认不可逆删除远程服务器中全部微信同步数据。
- 删除前核对：消息 4484、车单 1343、车辆明细 1366、人员明细 2405、附件记录 478、附件文件 1435。
- 已创建远程回滚备份：`/opt/plateview/backups/pre-wechat-purge-20260922T215807Z.dump`。
- 仅清空微信消息、来源、车单、车辆、人员和附件数据；表结构、迁移、用户、车辆档案及其他业务数据保留。
- 已清空远程附件目录文件；删除后上述表计数均为 0。
- 采集器状态已备份至 `.codex/backups/wechat-collector-before-full-resync-20260923-055859.json` 和 `.codex/backups/wechat-collector-before-remote-resync-20260923-060027.json`，并重置为空游标。
- 发现采集器原先指向本机 `127.0.0.1:8080`，已改为远程生产地址 `https://api.chenxiruyu.dpdns.org`，随后重新启动用户级 systemd 服务。
- 远程重同步已开始，已观察到消息与附件持续增长；采集器仍在运行，首轮尚未完全结束。
- 首轮全量同步已完成，三个群均上报 `HEALTHY`，远程计数为消息 4484、车单 1340、车辆明细 1357、人员明细 2400、附件记录 478、附件文件 1288。
- 附件状态为图片 460 条可用、PDF 16 条可用、PDF 2 条仅元数据；本机采集器仍保持运行，当前重试队列共 70 条（其中第二群 4 条、第三群 66 条）。
- 首轮同步期间未发现采集器错误或警告日志，服务状态为 enabled/active。
## 多架构 APK 与动态更新链路 - 2026-09-23

- Release 已启用 R8 和资源压缩，并配置 arm64-v8a、armeabi-v7a、universal 三种 APK 输出。
- GitHub 发布工作流已改为上传三种带架构名称的 Release 资产。
- Android 更新模型已增加架构选择、架构独立缓存文件名、文件大小和摘要校验；设备按 arm64-v8a、armeabi-v7a、universal 顺序选择。
- 服务器更新镜像脚本已改为逐个下载三个 APK，全部成功后原子写入多架构 latest.json。
- 管理员更新地址校验已改为验证三个 APK 的相对路径、范围请求、文件大小和 SHA-256。
- 本地验证：镜像脚本测试通过、Android 单元测试通过、服务端测试通过、Android Lint 通过、Release R8/资源处理/分包任务通过。
- 当前工作区没有发布签名文件，未执行签名发布或 GitHub 推送。

## 编码前检查 - 管理工作台、详情与附件缓存性能

时间：2026-09-23

- 已查阅上下文摘要：`.codex/context-summary-管理工作台详情与附件缓存性能.md`。
- 复用 `findCachedAttachmentFile`、`downloadAttachmentVariant`、`attachmentCacheKey`、Room 下载任务、WorkManager 调度和车辆详情本地优先状态模式。
- 遵循 Kotlin/Hilt/MVVM/StateFlow/Compose、Ktor/JDBC/Flyway 及中文测试名称约定。
- 已对比普通车辆详情、微信详情、附件 Worker、管理员附件和车辆管理分页五类实现，确认不重复造轮子。
- 当前环境未提供 `sequential-thinking` 和 `shrimp-task-manager` 工具，使用显式任务计划、上下文摘要和本地回归测试替代，不跳过验收。
- 验收优先级：本地详情首帧、附件零重复请求、同附件并发去重、管理概览单请求、微信异常分页与批量候选查询。

## 编码前检查 - 全量授权附件预取与点击即开

时间：2026-09-24

- 已查阅上下文摘要：`.codex/context-summary-全量授权附件预取.md`。
- 复用 `WechatAttachmentCacheRepository`、`downloadAttachmentVariant`、`WorkOrderCacheDao`、`WechatAttachmentCacheSyncScheduler` 和既有附件状态上报接口。
- 遵循 Kotlin、Room、Hilt、WorkManager、Retrofit、Ktor、JDBC 的现有命名与模块边界。
- 已对比 `RoomWorkOrderRepository.kt`、`WechatAttachmentCacheRepository.kt`、`WechatAttachmentCacheSync.kt`、`AttachmentVariantDownloaderTest.kt` 四个实现，确认不建立第二套缓存或下载状态源。
- 当前环境未提供 `sequential-thinking` 和 `shrimp-task-manager`，使用源码调用链审查、显式计划和本地自动测试完成等价检查，并在验证报告中记录该工具限制。
- 本轮验收重点：清单分页版本一致、权限撤销清理、原文件完整性校验、任意网络后台续传、前台提权、逐任务状态上报和缓存命中零网络请求。

## 编码后声明 - 全量授权附件预取与点击即开

时间：2026-09-24

- 复用了 `WechatAttachmentCacheRepository`、Range 下载器、Room 下载任务、WorkManager 调度、附件清单和状态上报接口，没有建立第二套缓存。
- Room 升级至 v5，任务主键与统一缓存键一致，并增加前台优先、进度、重试、来源质量和本地路径字段。
- 服务端新增 V42 单调附件清单修订值，避免分页期间全表聚合和版本漏变。
- 管理员、普通详情与后台 Worker 已统一到同一任务链路；应用恢复前台时立即继续附件同步。
- 双轴审查发现并修复旧任务遮挡、新旧状态覆盖、修订值不完整、全零权限旧统计、认证失败误删缓存和管理统计缺项。
- Android 单元测试、仪器测试源码编译、Lint、Debug 构建、服务端测试及差异检查均通过。

## 编码前检查 - 恢复普通管理员账号管理

时间：2026-09-24

- 已查阅 `.codex/context-summary-恢复普通管理员账号管理.md`。
- 复用 `AdminTab.Users`、`UsersPane`、`UserEditorState.canEditProfile` 与 `AdminUserProfilePolicy`。
- 对比了当前客户端入口、旧版本入口和服务端受限权限实现，确认无需新建权限体系。
- 当前环境未提供 `sequential-thinking` 与 `shrimp-task-manager`，使用 Git 历史、源码调用链和自动测试完成替代审查。

## 编码后声明 - 恢复普通管理员账号管理

时间：2026-09-24

- 恢复普通管理员的账号页签和概览卡片，未复制或新建账号管理页面。
- 轻量概览接口对全部管理员返回真实用户数量，完整列表仍只在进入账号页签后加载。
- 主管理员专属的排班、微信同步、数据访问和敏感账号资料管理保持不变。
- Android 定向测试、全量单元测试、Lint、Debug 构建与服务端测试均通过。
- 双轴远程审查因 429 与 503 失败，已记录并使用本地源码与测试完成补偿审查。
## 编码前检查 - 本地微信同步批次修复

时间：2026-09-24 12:06:00

- 已查阅上下文摘要：`.codex/context-summary-本地微信同步批次修复.md`
- 复用 `ensureSyncRun`、`findSyncBatch`、`transaction` 和 `recordSyncBatch`。
- 遵循现有 Kotlin 扩展函数、JDBC 资源关闭和单事务风格。
- 已检查 `WorkOrderService`、V40 迁移与采集器批次协议，确认不需要新增重复组件。
- 当前环境未提供 sequential-thinking、shrimp-task-manager 和 context7 工具，已通过本地调用链、数据库异常堆栈和现有测试框架完成等价分析并留痕。

## 编码后声明 - 本地微信同步批次修复

时间：2026-09-24 12:14:00

### 1. 复用了以下既有组件

- `ensureSyncRun`：在批次记录前幂等建立同步运行记录。
- `findSyncBatch`：在已确认批次重发时返回原结果。
- `transaction`：将消息写入、批次确认和水位更新保持在同一事务。

### 2. 遵循了以下项目约定

- Kotlin 标识符和 JDBC 扩展函数命名与原文件一致。
- 未引入新依赖、新接口或第二套批次实现。
- 修复位于既有 `ingest` 事务边界内。

### 3. 对比了以下相似实现

- `recordSyncBatch`：保留原有批次和水位写入协议，仅补齐前置条件。
- V40 迁移：严格遵循 `wechat_sync_batches.sync_run_id` 外键约束。
- Python 采集器：保留稳定 `batchId` 和 `syncRunId` 语义，服务端负责幂等接收。

### 4. 未重复造轮子的证明

- 已检查 `WorkOrderService`、`WorkOrderFeature`、V40 迁移和采集器批次协议。
- 修复直接激活已存在但未调用的两个帮助函数。

### 5. 本地运行结果

- 服务端单元测试全部通过。
- 本地 API 镜像重建并健康启动。
- 三个微信群首轮分别确认 298、1564、2762 条，下一轮新增均为 0。
- 本地库有 4624 条微信消息、1403 条车单、25 个已确认批次和 3 个群水位。
- 附件任务中 403 张原图和 16 个 PDF 已上传；70 张图片和 2 个 PDF 仅有元数据，等待本地原文件可用后补传。
## 编码前检查 - 附件缓存状态回流修复

时间：2026-09-24 12:25:00

- 已查阅 `.codex/context-summary-附件缓存状态回流修复.md`。
- 将复用 `observeAttachmentDownload`、`AttachmentDownloadState.localPath`、`AttachmentViewerDialog` 和 `getWechatSyncOverview`。
- 遵循现有 Kotlin `StateFlow`/`Flow` 和 Compose 单向状态流风格。
- 已对比聊天详情、车单详情、管理页、Room 附件任务和服务端异常附件查询，确认不新增缓存体系。
- 当前环境没有 sequential-thinking、shrimp-task-manager 和 context7 工具，已使用可重复的数据库查询、服务端日志和真机现象完成等价诊断并留痕。

## 编码后声明 - 附件缓存状态回流修复

时间：2026-09-24 13:05:00

- 复用现有 Room 附件任务、`observeAttachmentDownload`、统一缓存目录和详情页状态流，没有新增第二套附件缓存。
- 微信聊天详情和微信车单详情在正文出现后立即订阅每个附件任务；`COMPLETED + localPath` 会恢复为真实本地文件并写入页面文件映射。
- 本地路径只有在文件存在且非空时才视为缓存命中，避免损坏记录导致空白页面。
- 附件缩略区域在尚未取得文件时改为明确的“附件正在后台缓存”，不再无条件显示无限转圈。
- 原文件尚未被采集器取得时不再发起无效原文件请求，页面保留明确的等待补传状态。
- 管理员微信同步页首次加载已接入真实完整性与客户端缓存统计，不再停留在默认零值。
- 定向回归测试、Android 全量单元测试、Android Lint、Debug APK 构建和服务端测试全部通过。

## 编码前检查 - 退出登录稳定性修复

时间：2026-09-24 13:20:00

- 已查阅 `.codex/context-summary-退出登录稳定性修复.md`。
- 将复用 `AuthRepository.session`、DataStore 原子编辑与 `WorkOrderRepository.clear(userId)`。
- 遵循现有 Kotlin 协程、Hilt 单例仓库和根界面会话驱动导航模式。
- 已对比确认弹窗、应用会话 ViewModel、认证仓库、资料更新退出和缓存互斥清理五处实现。
- 确认问题不是按钮点击丢失，而是缓存清理位于会话删除之前，且其等待或失败会阻断退出。

## 编码后声明 - 退出登录稳定性修复

时间：2026-09-24 13:35:00

- 复用了 `AuthRepository.session`、认证 DataStore 和现有当前账号缓存清理入口。
- 退出顺序改为先原子清除登录会话，再尽力清理当前账号缓存。
- 缓存同步持锁、文件删除失败或 Room 清理异常不再阻止令牌删除和登录页切换。
- 运行时后台地址、更新地址和客户端实例配置仍保存在独立 DataStore，退出不会删除。
- 新增三个回归测试，覆盖调用顺序、缓存清理失败和无当前账号三种情况。
- Android 全量单元测试、Lint、Debug APK 构建和差异空白检查全部通过。

## 编码前检查 - 30秒有界一致性

时间：2026-09-24 14:00:00

- 已查阅 `.codex/context-summary-30秒有界一致性.md`。
- 将复用车辆 generation 快照、微信 revision 增量、附件 manifest、现有 profile 心跳和 WorkManager。
- 遵循 Ktor/JDBC/Flyway 与 Hilt/Room/StateFlow 的现有模块边界。
- 已分析车辆、微信车单、聊天记录、附件、认证心跳和运行时策略六条集成路径。
- 确认不新增第二套15秒目录轮询；版本一致时不得产生增量请求。
- 当前环境未提供 sequential-thinking、shrimp-task-manager 和 context7，使用源码调用链、显式计划和本地自动测试完成等价分析并留痕。

## 编码后声明 - 30秒有界一致性

时间：2026-09-24 14:55:00

### 1. 复用了以下既有组件

- 车辆 generation 快照与 `VehicleCacheRepository.synchronizeCatalog`：保留原子 generation 切换。
- 微信目录 Room、分页同步和 `WorkOrderRepository.synchronize`：增加固定目标版本与单事务最终提交。
- 附件 manifest、Room 任务和 WorkManager：版本变化后继续复用统一下载队列。
- `/auth/profile`、认证 DataStore 与 Retrofit 单例：作为唯一高频前台确认链路。

### 2. 遵循了以下项目约定

- 服务端使用 Ktor、JDBC、Flyway 和单行状态表；Android 使用 Hilt、StateFlow、DataStore、Room 和 WorkManager。
- 首页搜索继续只查询本地 Room；详情命中本地缓存后立即展示，后台状态不阻塞正文与附件。
- 用户可见一致性状态和测试名称均使用简体中文。

### 3. 已实现的关键行为

- 新增 V43 单行目录状态镜像，车辆、微信和附件源修订通过数据库触发器同步。
- 登录与 `/auth/profile` 返回账号可见的四类目录版本及策略版本；无权限目录返回不可访问状态。
- 前台会话心跳改为串行 12～18 秒随机间隔，后台停止；网络恢复立即检查，15 分钟 Worker 兜底。
- 5 秒内最近成功目录结果复用，版本一致时协调器不调用目录同步。
- 同账号同步请求合并，全局最多两个目录网络任务；微信车单和聊天共用一次目录同步。
- 首页、车辆详情、微信车单详情和聊天详情显示“已确认、确认中、同步中或未确认”。
- 微信目录分页下载完成后才通过 Room 事务写入数据和推进版本；服务端分页锁定 `targetRevision`。

### 4. 本地验证结果

- 服务端全量测试、Android 全量单元测试、Android Lint 和 Debug APK 构建通过。
- V43 在本地 PostgreSQL 容器成功迁移，源版本与单行镜像一致；事务回滚后无漂移。
- 持续 67 次/秒、1005 次 `/auth/profile` 请求：零错误，P50 3.67 毫秒、P95 6.17 毫秒、P99 7.11 毫秒。
- 67 并发突发、1000 次请求：零错误、吞吐 411.93 次/秒，但 P95 887.52 毫秒，作为突发容量风险保留。

### 5. 尚未完全落地的计划项

- 车辆仍使用 generation 全量重建，尚未实现车辆变更日志和增量墓碑。
- 微信服务端尚未提供 DELETE/REVOKE 墓碑与 90 天日志保留边界，删除数据需后续迁移补齐。
- 多实例 PostgreSQL LISTEN/NOTIFY 尚未实施；当前单实例使用 5 秒内存快照。
- 管理端客户端一致性统计、真机 30 秒端到端故障注入和 OPPO 性能验收尚未执行。

## 编码后声明 - 30秒有界一致性完整实现

时间：2026-09-24 15:45:00 CST

- 复用 `/auth/profile`、`ClientRuntimeCoordinator`、车辆 generation、微信 Room 事务、附件任务队列和现有 WorkManager，没有新增第二套周期轮询。
- 新增 V43 单行目录状态、V44 三类变更日志和 V45 实体创建/当前修订号；业务写入与修订号、变更日志保持同一数据库事务。
- 固定目标分页允许目标之后新增无关实体继续同步；目标之前的实体若再次修改则返回 409，客户端不推进本地版本。
- 车辆完整目录改为数据库分页，不再每页装载完整目录；车辆 Room V8 按账号隔离并清空无法归属的旧缓存。
- 最近目录状态按账号缓存并在退出时清除；附件清单与文件下载拆锁，撤权删除也复用附件键级互斥。
- 变更日志每6小时清理90天以前的非最新边界记录，清理使用独立事务，不占用用户心跳请求。
- 当前环境没有 `sequential-thinking` 与 `shrimp-task-manager`，已用项目计划、上下文摘要、双轴审查和本地故障注入完成等价流程。

## 验证记录 - 30秒有界一致性

时间：2026-09-24 15:45:00 CST

- 服务端全量测试通过；Android 全量单元测试、Lint、Debug APK 构建通过。
- OPPO RMX3461 执行车辆与微信 Room 数据库测试共14项，全部通过。
- 本地 Docker 镜像按代理规范构建成功，V43、V44、V45 迁移成功，`/health` 与目录状态接口正常。
- 数据库约束验证：三类实体当前修订号均不低于创建修订号，三类日志创建修订号均非空。
- 故障注入验证：目标后新增车辆和消息时旧目标请求返回200；旧实体更新后旧目标请求返回409。
- 90天清理验证：过期非边界日志删除，最新边界日志保留，事务回滚后无测试数据污染。
- 临时空数据库从V1到V45连续执行38个Flyway迁移全部成功，验证V45不依赖本地历史状态。
- 压测复用此前结果：持续约67次 profile 请求/秒零错误，P95 6.17毫秒；突发67并发时P95 887.52毫秒，保留为容量监控项。

## 编码前检查 - 微信车单早八晚十一

时间：2026-09-24 15:51:13 CST

- 已查阅 `.codex/context-summary-微信车单早八晚十一.md`。
- 将复用 `evaluateWorkOrderPassageValidity`、`resolveWorkOrderPassageState`、`vehicleTypeForPlate` 和 `selectPassageTimeRemarkForVehicle`。
- 遵循现有 Kotlin 领域函数、北京时区和 JUnit 中文测试命名约定。
- 已对比领域格式化器、首页候选卡、详情页状态标签、服务端车单解析器和现有边界测试。
- 确认不新建第二套通行判定，通过统一领域层覆盖首页与详情页。
- 当前环境没有 `sequential-thinking`、`shrimp-task-manager` 和 `context7` 工具，已使用源码调用链、显式任务计划和本地自动测试执行等价流程并留痕。

## 编码后声明 - 微信车单早八晚十一

时间：2026-09-24 16:28:00 CST

### 1. 复用与实现结论

- 复用 `evaluateWorkOrderPassageValidity`、`resolveWorkOrderPassageState`、`vehicleTypeForPlate` 和 `selectPassageTimeRemarkForVehicle`，没有在首页或详情页增加第二套判定逻辑。
- 现有通用中文时间解析器已经能够把“晚十一”解析为 23:00，因此通过完整业务文本回归测试固定新规则，不增加整句硬编码。
- 混合车型车单按当前搜索命中的车牌查找 `vehicles` 中对应车型；轻型车只使用轻型时段，重型车只使用重型时段，无车型前缀的午间时段作为两类车公共窗口。
- “车辆不得停靠三湾”不含时间提示，不进入时间窗口计算，但原始备注仍可在详情中展示。

### 2. 边界语义

- 轻型车“早八晚八”：00:00 至 07:59:59、20:00 至 23:59:59有效。
- 重型车“早八晚十一”：00:00 至 07:59:59、23:00 至 23:59:59有效。
- “中午两点到四点”：14:00 至 16:00:59为两类车公共有效窗口。
- “晚十二”保持午夜边界语义，不把当日23:59误判为晚十二之后；次日零点自然落入早八前窗口。
- 日期未开始、日期已过期、当日小时不匹配、无法解析分别显示“通行时间未开始”“通行时间已过期”“不在通行时间”“通行时间待核实”。

### 3. 本地验证

- 通行时间领域定向测试通过。
- Android 全量单元测试、Lint 和 Debug APK 构建通过。
- 服务端全量测试通过。
- `git diff --check` 通过。
- OPPO 首次 ADB 握手收到异常长度数据包，重启并恢复 Android SDK ADB 后设备重新上线；新增 Room 撤权仪器测试6项全部通过。
- 已覆盖安装 `app-arm64-v8a-debug.apk`，设备版本为0.3.30；应用进程存活、`MainActivity` 位于前台，启动日志无致命异常。

## 编码后声明 - 30秒有界一致性最终闭环

时间：2026-09-24 16:28:00 CST

- 新增 V46，拆分微信车单与聊天消息的对外目录版本；任一聊天变化不再无条件触发车单同步。
- 权限撤销会清空对应本地数据并把应用修订、确认时间和同步时间归零；策略版本变化会按新权限重新构建缓存。
- 附件关联、忽略和重新关联会刷新原归属与新归属实体修订，避免附件目录更新后详情元数据保持陈旧。
- 放行发送者别名或启用状态变化时，在同一事务重新分类历史消息并刷新受影响的消息和车单修订；无受影响数据时不空推进目录版本。
- V1 至 V46 的39个迁移已在临时空数据库连续执行通过；V46事务回滚和车单、聊天版本独立性已由真实 PostgreSQL 验证。

## 编码前检查 - 无微信权限目录误同步修复

时间：2026-09-24 16:55:00 CST

- 已查阅 `.codex/context-summary-无微信权限目录误同步修复.md`。
- 将复用 `CatalogStateResponse`、负修订号撤权协议、`CatalogFreshness.revoked`、账号微信权限和现有首页状态流。
- 遵循服务端 Kotlin Test 与 Android JUnit、协程测试的既有风格。
- 已对比目录状态服务、认证响应、客户端一致性协调器、运行时缓存重建和首页搜索状态五个实现。
- 确认不存在第二套目录权限策略；修复将收敛到服务端目录可见性和客户端统一状态过滤。
- 当前环境没有 `sequential-thinking`、`shrimp-task-manager` 和 `context7`，已用复现日志、源码调用链、显式计划和测试驱动流程完成等价分析并留痕。

## 编码后声明与本地部署 - 无微信权限目录误同步修复

时间：2026-09-24 17:24:06 CST

- 服务端目录可见性同时检查账号微信访问开关和两类全局结果上限；不可访问目录统一返回负修订号。
- Android 一致性协调器对旧服务端返回值增加账号权限兜底，无微信权限时不会请求微信车单、聊天记录和附件目录。
- 首页确认状态只聚合当前账号实际可见且结果上限大于零的目录，微信撤权状态不会污染车辆目录确认结果。
- 服务端全量测试以及 Android 单元测试、静态检查、调试包构建全部通过。
- 按项目代理规范重建 `plateview-api` 镜像，使用 `docker compose --env-file .env up -d --no-build` 替换接口容器，保留现有数据库容器与数据卷。
- 本地 `/health` 返回 200；接口服务启动日志无迁移或数据库错误。
- 已在真机 `PKM110` 安装 0.3.30 arm64 调试包，配置 `tcp:8080` 反向映射并启动主界面；进程存活，精确崩溃日志为空。

## 编码前检查 - 设备级微信附件共享缓存

时间：2026-09-24 17:35:00 CST

- 已查阅 `.codex/context-summary-设备级微信附件共享缓存.md`。
- 将复用 `downloadAttachmentVariant`、`findCachedAttachmentFile`、账号级附件任务表、授权清单同步和附件键级互斥。
- 遵循现有 Kotlin 命名、JUnit4 中文测试名称、临时目录测试和 Room 按账号查询约定。
- 已对比附件磁盘仓库、Room 工作单仓库、附件任务 DAO、认证退出流程、运行时远程清缓存五个实现。
- 确认不新增第二套下载器或授权表；物理文件按摘要共享，账号任务继续作为唯一可见性索引。
- 当前环境没有 `sequential-thinking`、`shrimp-task-manager` 和 `context7`，已使用源码调用链、项目计划、上下文摘要和本地测试完成等价流程并留痕。

## 编码后声明 - 设备级微信附件共享缓存与0.3.31发布

时间：2026-09-24 18:05:00 CST

### 1. 复用与实现

- 继续使用现有授权附件清单、账号级 Room 下载任务、断点续传、SHA-256 校验和附件键级互斥。
- 只有带合法 SHA-256 的原始文件进入 `files/work-order-images/shared`，无摘要文件和临时分片继续按账号隔离。
- 新账号必须先取得服务端授权清单并创建自己的 Room 任务，之后才能获得共享文件路径；微信正文、车单详情和搜索索引没有跨账号共享。
- 旧账号目录中的文件在首次授权命中时重新校验大小和摘要，验证通过后复制到共享目录。
- 普通退出只清除认证会话，保留账号缓存和共享文件；权限撤销删除账号任务和业务缓存但保留共享实体；远程清缓存显式删除设备共享附件。

### 2. 版本发布

- Android `versionCode` 从34升级为35，`versionName` 从0.3.30升级为0.3.31。
- README 当前版本同步更新为0.3.31。
- GitHub 发布工作流无需修改，标签发布仍生成 arm64-v8a、armeabi-v7a 和 universal 三个正式签名 APK。

### 3. 本地验证

- 共享缓存真机仪器测试4项全部通过。
- Android 全量单元测试、静态检查和调试 APK 构建通过。
- 服务端全量测试通过。
- 未执行本地正式签名构建；正式签名仅由 GitHub 受保护密钥完成。
## 真机验证 - 主管理员远程清理用户缓存

时间：2026-09-24 22:05:00 CST

- 已检查管理端入口、服务端修订号、客户端消费、缓存清理与回执链路，确认功能未被删除。
- 在本地环境创建一次性用户 `cache_reset_probe`，并由 OPPO PKM110 的 `0.3.31` 调试包真实登录。
- 清理前在应用私有目录植入账号附件、设备共享附件、头像和临时缓存四类探针。
- 主管理员请求返回修订号 `1`、目标设备 `1`、完成设备 `0`、状态 `SENT`。
- 真机随后删除全部四类探针，回执修订号变为 `1`；管理状态变为 `COMPLETED`，完成设备 `1/1`。
- 登录主界面仍然存在，客户端实例编号、接口地址和更新地址保持不变。
- 清理完成后新建探针并等待二十秒，文件仍存在，证明同一修订号不会重复执行清理。
- 数据库审计记录存在 `CLIENT_CACHE_RESET_REQUEST`，目标用户编号为 `15`。
- 测试结束后已删除一次性用户、刷新会话、客户端登记、相关审计记录和全部探针，未触发生产环境。
- 一次组合命令因包含被执行环境禁止的 `rm -f` 写法而未运行；改用 `unlink` 后完成清理，没有影响测试结论。
- 当前环境没有 `sequential-thinking` 与 `shrimp-task-manager`，已使用源码调用链、真实接口、数据库状态和真机文件系统完成等价验证并留痕。
## 编码前检查 - 首页吸顶微信状态与缓存分页

时间：2026-09-24 22:30:00 CST

- 已查阅 `.codex/context-summary-首页吸顶微信状态与缓存分页.md`。
- 将复用 profile 前台心跳、采集来源状态、附件状态上报、车辆候选徽标和现有 Compose 列表结构。
- 遵循 Kotlin 标识符、中文测试名称、Material3主题色和 Flyway 连续迁移约定。
- 已检查首页搜索、三类详情、管理员微信同步、采集心跳和附件缓存汇总模块，确认不存在可直接满足完整需求的既有分页或聚合健康状态实现。
- 保留车辆候选卡片的“核验就绪/数据未确认”标签，仅删除首页全局框和三类详情框。
- 当前环境缺少强制流程中列出的 `sequential-thinking`、`shrimp-task-manager`、`context7` 和 GitHub代码搜索工具，已记录限制并采用项目源码、现有测试及本地验证替代。

## 编码前检查 - 微信标题单行状态布局

时间：2026-09-25 00:10:00 CST

- 已查阅 `.codex/context-summary-微信标题单行状态布局.md`。
- 将复用 `SearchStickyHeader`、`WechatSyncHealthBadge`、`SearchSectionStatus` 和既有首页 Compose 测试。
- 遵循 Compose `Row`、稳定宽度、Material3 容器颜色和中文测试名称约定。
- 已确认服务端五态只需在首页折叠为两态，不修改 profile 协议和健康状态仓库。
- 目标是修正截图中的标题换行、状态块过宽和数量位置，不新增网络请求。

## 编码后声明 - 首页吸顶微信状态与缓存分页

时间：2026-09-24 23:39:12 CST

### 1. 复用了以下既有组件

- `AuthRepository.validateSessionLocked()`：继续复用现有前台 profile 心跳写入微信同步健康状态，没有新增周期请求。
- `SearchScreen` 单一 `LazyColumn`：三类结果和搜索历史直接升级为原生吸顶标题，车辆候选原有核验标签保持不变。
- `RoomWorkOrderRepository` 附件缓存状态上报：服务端在同一事务从既有明细刷新客户端汇总行，没有新增第二套上报协议。
- `AdminWorkspaceViewModel` 页签加载和错误处理：缓存翻页使用独立仓库方法，切页不重新加载异常附件。

### 2. 遵循了以下项目约定

- Kotlin DTO、领域模型、Retrofit 和 Ktor 响应字段保持同名映射。
- Flyway 新增连续迁移 V47、V48，并在独立临时 PostgreSQL 数据库验证连续迁移及回填。
- Compose 使用 Material3、现有主题色和 `PlateViewDimensions`，所有新增界面文案与测试名称使用简体中文。
- 本机设备按当前管理员编号与客户端实例编号共同识别，分页接口继续限制为 `admin + ADMIN`。

### 3. 对比了以下相似实现

- 首页车辆、微信车单和聊天分组：继续共用同一列表，差异仅是标题改成吸顶并为微信分类增加健康状态。
- 车辆详情、车单详情和聊天详情：只删除顶部确认状态显示，保留本地优先读取、后台核验和目录变更刷新逻辑。
- 管理端原有客户端前八条展示：替换为本机摘要和服务端分页，异常附件、发送者配置和逐来源状态保持原逻辑。

### 4. 未重复造轮子的证明

- 检查了认证心跳、目录一致性、采集器状态、附件缓存上报和管理员工作台模块，确认复用现有网络与状态链路。
- 没有新增轮询器、缓存下载器或附件明细表，只新增健康状态仓库和数据库汇总表。

### 5. 验证与异常处理

- 服务端全量测试通过；Android 单元测试、Lint、Debug APK 构建和仪器测试源码编译通过。
- V1 至 V48 的 41 个迁移在独立临时数据库连续执行通过；V47 `last_uploaded_at` 回填验证通过。
- 1000 台模拟设备分页查询执行时间约 1.3 毫秒，5 秒压测平均延迟约 1.8 毫秒，无超过 100 毫秒请求。
- OPPO PKM110 安装最新 arm64 Debug APK，本需求 9 项定向仪器测试全部通过；应用启动进程存活且没有致命崩溃日志。
- 初次仪器测试暴露测试代码重复 `setContent`、旧懒列表定位方式和一次 APK 路径拼接问题，均已修正并重新验证通过；未发现对应业务功能故障。

## 编码后声明 - 微信标题单行状态布局

时间：2026-09-25 00:12:00 CST

### 复用组件

- `SearchStickyHeader`：统一承载三类结果标题，改为三列单行排列。
- `WechatSyncHealthBadge`：继续使用既有北京时间格式化，压缩为绿色正常和红色异常两态。
- `WorkOrderScreenTest`：沿用现有真机 Compose 测试入口，覆盖五种后端状态。

### 变更边界

- 未修改服务端协议、profile 心跳、健康状态仓库或网络请求。
- 未修改车辆候选“核验就绪/数据未确认”标签。
- 只调整首页微信标题的布局、文案字号、颜色映射和回归测试。

### 验证结果

- 单元测试、Android 测试编译、Debug APK 构建均通过。
- OPPO PKM110 真机 `WorkOrderScreenTest` 19 项全部通过。
- 首次真机测试发现日期固定断言不稳定，改为断言“最后同步：”前缀后重新通过；不是业务故障。

## 编码后声明 - 同步时间显示具体日期

时间：2026-09-25 00:25:00 CST

- 将 `formatWechatSyncTime` 从“今天/日期分支”统一调整为 `M月d日 HH:mm`。
- 回归测试固定验证 `最后同步：9月24日 22:20`，覆盖日期和分钟。
- Debug APK 已安装到 OPPO PKM110，`WorkOrderScreenTest` 19 项通过。
## 编码前检查 - 0.3.31 闪退修复

时间：2026-09-25 01:30:00 CST

□ 已查阅上下文摘要文件：`.codex/context-summary-0.3.31闪退修复.md`
□ 将使用以下可复用组件：
  - `CatalogFreshness`：持久化同步失败状态和错误码。
  - `CatalogConsistencyCoordinator.persist`：统一写入状态，避免异常泄漏。
  - `CatalogReconcileActionTest`：沿用现有 JUnit4 测试模式。
□ 将遵循命名约定：Kotlin 类型使用大驼峰，函数和测试使用小驼峰。
□ 将遵循代码风格：协程取消异常单独捕获，其余异常转为 `FAILED` 状态。
□ 确认不重复造轮子：已检查目录协调器、搜索状态映射和现有一致性测试，复用现有状态模型。

## 编码后声明 - 0.3.31 闪退修复

时间：2026-09-25 02:54:00 CST

### 1. 复用了以下既有组件

- `CatalogFreshness`：记录失败状态、服务器观察版本和本地已应用版本。
- `CatalogConsistencyCoordinator.persist`：统一持久化失败结果。
- `CatalogReconcileActionTest`：沿用现有 JUnit4 测试夹具和中文行为测试命名。

### 2. 遵循的项目约定

- 协程取消异常单独处理，不吞掉 `CancellationException`。
- 网络或版本校验异常通过 `FAILED` 和 `lastErrorCode` 降级，不阻塞本地缓存读取。
- Debug 构建、单元测试和真机验证均使用项目现有 Gradle/ADB 流程。

### 3. 对比相似实现

- 与 `catalogReconcileAction` 的回退保护一致：版本异常只落状态，不推进版本。
- 与 `SearchViewModel` 的失败展示约定一致：`FAILED` 映射为“数据未确认，请谨慎核验”。

### 4. 未重复造轮子的证明

- 检查了目录协调器、车辆缓存、微信仓库和一致性测试，没有新增同步队列或第二套状态模型。
## 编码前检查 - 微信车单主记录与年份隔离

时间：2026-09-26

- 已查阅 `.codex/context-summary-微信车单主记录与年份隔离.md`。
- 复用 `WorkOrderParser`、目录修订日志、附件双归属字段、Room原子同步和`PlateQueryNormalizer`。
- 遵循现有Kotlin、Flyway、Room、Compose与JUnit测试模式。
- 已检查解析、入库、搜索、目录增量、附件关联、Room缓存和候选卡片，确认不存在可直接满足年份主记录归并的重复实现。
- 当前环境未提供sequential-thinking与shrimp-task-manager工具，已用代码调用链、数据协议和迁移依赖的结构化审查替代并记录。

## 编码前检查 - 其他长期风险车辆候选权限例外

时间：2026-09-26

- 已查阅`.codex/context-summary-微信车单主记录与年份隔离.md`新增的车辆权限分析。
- 复用`VehicleAccessScope`、车辆目录修订与`REVOKE`墓碑、`RoomVehicleCacheRepository`和`SearchViewModel.selectCandidate`。
- 遵循服务端Ktor/Kotlin同名响应字段、Android DTO到领域模型映射、Room显式迁移及中文JUnit测试命名。
- 已检查搜索、精确车牌、普通目录、全量目录、增量目录、详情接口、Room缓存和Compose候选卡片，确认没有可直接满足“候选可见但详情不可访问”的既有字段或分支。
- 依赖关系：车辆状态和类别决定候选可见性，访问范围决定详情可见性，目录脱敏记录进入Room后由候选点击标记阻止导航。
- 当前环境未提供sequential-thinking、shrimp-task-manager、Context7和GitHub代码搜索工具；本需求是项目专属权限规则，不依赖新增外部库，已用本地调用链检索、现有测试模式和任务计划替代，并保留记录。

## 编码后声明 - 其他长期风险车辆候选权限例外

时间：2026-09-26

### 已复用组件

- `VehicleAccessScope`与车辆目录修订：区分候选可见和详情可见，不新增第二套同步接口。
- `VehicleDetail.filteredFor`与目录增量墓碑：普通详情继续严格过滤，风险状态恢复后通过`REVOKE`移除本地候选。
- `RoomVehicleCacheRepository`与`SearchViewModel.selectCandidate`：缓存保存详情访问标记，点击在历史写入和导航前静默拦截。
- `InactiveVehicleStatusBadge`：继续显示拉黑、严查状态，不新增权限提示文案。

### 约定与差异

- 服务端候选、普通目录、全量目录和增量目录统一允许无权限账号看到`BLACKLISTED`、`STRICT_CHECK`车辆；详情接口仍使用原严格条件。
- 受限目录记录只保留车牌、类别、状态等候选信息，车辆属性、人员档案和单位详情全部脱敏。
- Android车辆缓存从8迁移到9，旧记录默认可访问；Retrofit字段使用可空布尔并在映射层回退为`true`，兼容未返回新字段的旧服务端。
- 候选卡片禁用点击并隐藏箭头，历史记录对其他长期车辆二次检查缓存详情，未授权时无任何导航或提示。

### 验证与异常处理

- 服务端`./gradlew test`通过。
- Android`:app:testDebugUnitTest`、`:app:assembleDebugAndroidTest`和`:app:assembleDebug`通过。
- OPPO RMX3461真机`:app:connectedDebugAndroidTest`定向26项通过，0失败；初次自动安装因同时传入多个ABI APK导致split重复，改用universal APK手动安装后通过。
- `git diff --check`通过；未发现编译错误、测试失败或格式问题。

## 计划复核与补充 - 微信车单三项需求

时间：2026-09-26

- 逐项核对了聊天匹配车牌、同来源同北京时间年份动态归并、附件双归属、目录增量、Room 6到7迁移和历史查询实现。
- 发现一个到达顺序缺口：附件先于微信消息上传时，原逻辑只在附件上传时反向找消息，消息后来入库不会再找待关联附件。
- 已在`WorkOrderService.ingest`增加事务内反向关联：消息入库后按来源、发送者和前后120秒查找唯一待关联附件，同时设置车单记录归属和聊天消息归属；详细车单成为主记录后继续由既有归并逻辑切换到详细车单并保留简短消息附件引用。
- 附件不复制文件，仍复用`linked_record_id`和`linked_message_id`；多个候选时保持未关联，避免错误跨单号绑定。
