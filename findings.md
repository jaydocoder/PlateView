# PlateView 发现与决策

## 已确认需求

- 项目名称与应用显示名为 `PlateView`，包名为 `com.jaydocoder.plateview`。
- 普通用户登录后，可手动输入或通过系统语音输入车牌。
- 搜索框支持车牌模糊匹配；用户点击候选车牌后进入车辆详细核验页。
- 普通用户查看车辆详情和本机搜索历史；不需要车辆详情离线缓存。
- 管理员可管理车辆数据、用户账号、角色和 Excel 批量导入。
- 初始管理员用户名固定为 `admin`；密码由部署配置提供。
- 首版交付调试 APK、正式签名 APK、Ktor 后端和 Docker Compose。

## 数据发现

- `2026年哈纳斯村村民车辆统计表7.6更新.xlsx`：村民车辆，字段包括姓名、身份证号、车牌号、联系方式和备注。
- `2026年驻景区长期通行工作用车统计表（汇总全）.xlsx`：包含驻景区单位、驻景区企业、干部车辆和旅游发展股份有限公司车辆等工作表。
- 长期车辆表有单位名称跨行继承、单元格内多车牌、不同字段列数和部分不规范记录。
- 数据导入必须保留源文件、工作表、行号、原始值与规范化结果，保证可追溯。

## 数据治理规则

- 使用 `normalizedPlate` 作为检索键：大写后移除空格、中点、连字符等格式符号。
- 原始车牌始终保留，用于结果展示与数据核查。
- 导入将记录分为新增、更新、重复、异常；管理员确认后才发布到正式库。
- 同一规范化车牌冲突时不得自动覆盖，必须显示来源和差异供管理员处置。
- 多车牌单元格先提取可识别车牌；无法无歧义拆分时标记异常并等待人工确认。

## 查询与审计规则

- 归一化后有效输入至少为 4 个字符；请求采用 250 毫秒防抖。
- 候选最多 20 条，排序为精确、前缀、包含匹配。
- 候选仅显示车牌和车辆所属类型；姓名、身份证号和联系方式只在已登录用户点击详情后展示。
- 每次详情查询记录账号、时间、规范化车牌、结果状态和设备摘要；不记录身份证号或联系方式。
- 本机搜索历史只保存当前账号、车牌、类别和时间，支持删除单条与清空。

## 技术与体验决策

| 决策 | 理由 |
| --- | --- |
| PostgreSQL `pg_trgm` GIN 索引 | 支持包含式车牌模糊查询。 |
| 乐观锁版本号 | 防止两个管理员互相覆盖车辆或账号编辑结果。 |
| Room 仅保存搜索历史 | 遵守不缓存车辆详情的需求。 |
| 系统语音识别 | 降低首版部署与维护成本，保留手动修正。 |
| Compose 检索终端风格 | 优先现场扫视、单手操作和状态辨识。 |

## 阶段 9：验收环境发现

- Docker Compose 是数据库迁移、真实工作簿导入、搜索、详情、管理员、审计和回滚端到端验收的标准路径；执行时使用独立项目名与临时卷，结束后删除。
- Android 仪器化测试已具备 Compose 与 Room 源码，必须由 `connectedDebugAndroidTest` 在解锁 Android 12 设备执行，不得将 APK 编译成功视作运行通过。
- 当前 ADB 可识别设备 `83bdbca2`（Android 12），但通知栏占据焦点，系统锁屏使测试 APK 安装确认无法继续。
- 当前项目与常见本机位置均未发现可用发布签名配置；不允许自行创建或伪造正式发布身份。
- 本机 Docker 29.7.1、Docker Compose 5.4.0、`curl` 与 `jq` 可用；`compose.yaml` 将 Flyway 迁移配置为 API 启动时自动执行，适合用新卷进行端到端迁移验收。
- 隔离环境中 4 条 Flyway 迁移生成 9 张业务表；两份真实工作簿完成预览、确认发布和回滚后，活动车辆数量回到 0，两个批次均为已回滚状态。
- 账号状态协议与车辆状态不同：账号仅使用 `ACTIVE`、`DISABLED`，车辆使用 `ACTIVE`、`INACTIVE`。Android 管理端选项和 API 契约均已正确使用该约定。
- 已认证模糊查询连续 30 次采样的 P95 为 0.005884 秒，满足小于 1 秒的目标。
- Android 12 真机 `83bdbca2` 已完成 8 个 Compose 与 Room 仪器化测试；调试应用在 ADB 端口映射的隔离 API 下完成登录、查询、详情、历史、管理员工作台、普通用户入口隐藏和断网恢复。
- 真机冷启动三次的中位数为 945 毫秒，运行期总 PSS 为 141429 KB。
- 调试 APK 使用 Android 自动提供的调试签名，可直接安装用于本地和真机测试；正式签名仅在后续分发、升级连续性和发布渠道需要时配置。
- ColorOS 对由 ADB 安装的测试 APK 会显示未知来源确认页。若 Gradle 在确认完成前启动运行器，会报告找不到目标包；完成确认后，先以 `adb install -r -t` 安装调试 APK，再以 `adb shell am instrument -w -r` 启动运行器，已在 Android 12 真机稳定通过 8 个测试。
- 本轮隔离 Docker Compose 环境已按独立项目名清理。默认 `plateview` Docker 卷与网络创建时间早于本轮验收，无法证明是临时数据，因此保持停止状态而不删除，避免误删本地开发数据。

## 阶段 10：生产部署发现

- 域名 `api.plateview.top` 的 A 记录指向 `47.96.190.39`，并由 Cloudflare 代理；部署必须验证 Cloudflare SSL 模式、阿里云安全组和回源端口后才可确认公网 HTTPS。
- 服务器 SSH 别名为 `aliyun`，以 `root` 用户连接；Docker `29.6.1`、Docker Compose `5.3.0` 可用，系统磁盘可用约 32G，内存总量约 1.6GiB。
- 当前服务器没有 Caddy 或 Nginx，仅监听 SSH 端口；存在持续运行的 `mysql80` 容器，PlateView 部署必须保持独立目录、项目名、网络和卷。
- 当前开发 Compose 将 PostgreSQL 和 API 映射到主机端口，只适用于本地开发。生产编排必须使用独立的 Caddy 反向代理，并且不得将 PostgreSQL、Ktor API 直接映射到公网。
- Android 默认 API 地址是模拟器地址 `http://10.0.2.2:8080/`，正式 APK 必须在构建时设置 `plateviewApiBaseUrl=https://api.plateview.top/`。清单未允许明文 HTTP，因此正式服务必须使用可验证的 HTTPS。
- 当前公网 DNS 解析为 Cloudflare 边缘地址 `104.21.58.220` 与 `172.67.209.17`。`http://api.plateview.top/health` 返回 Cloudflare `521`，HTTPS 握手失败；这是尚未部署可回源 HTTPS 服务的预期状态，不是应用接口错误。
- 服务器当前没有交换空间。Ktor 镜像会在 Docker 构建阶段运行 Gradle，1.6GiB 内存下应先配置交换空间或采用预构建镜像。
- 服务器执行 `git ls-remote https://github.com/jaydocoder/PlateView.git main` 在 20 秒后超时，不能作为当前部署路径。使用本机已验证 SSH 通道上传经 Git 校验的源码归档作为部署回退；服务器 GitHub 出站网络问题单独记录处理。
- 服务器对 `registry-1.docker.io:443` 的连接超时，不能在服务器直接拉取 Caddy 等镜像。生产部署改为在本机获取镜像、以 Docker 归档经 SSH 上传并在服务器执行 `docker load`。
- 生产服务已部署至 `/opt/plateview`：PostgreSQL、Ktor API 和 Caddy 运行正常，Flyway 已应用 4 条迁移；Caddy 为 `api.plateview.top` 签发 Let’s Encrypt 证书，服务器公网地址直连 HTTPS 健康检查通过。
- Cloudflare 橙云代理持续返回 `525`，但阿里云源站的公网 TLS、证书主题和健康端点均通过，问题仅在 Cloudflare 到源站的握手链路。为使 Android 立即可用，可将 API 记录切换为“仅 DNS”（灰云）；域名与 HTTPS 证书保持不变。
- 用户已将 API 记录切换为灰云。权威 DNS 与服务器递归 DNS 已返回 `47.96.190.39`；部分公共递归 DNS 仍缓存旧的 Cloudflare 边缘地址，真机联网验证需等待该缓存自然刷新。
- 用户将服务器 `.env` 的管理员密码改为 `123456` 后，已同步更新仅 `admin` 账号的 BCrypt 密码哈希并重建 API。初始管理员登录在源站 Caddy 路径验证通过。
- 当前 `main` 的界面美化提交包含一个缺失 `sp` 导入和三处过时 Material 3 `TextFieldDefaults.colors` 参数；已修复并通过 Android JVM 测试、Lint 与正式 HTTPS 地址调试 APK 构建。

## 阶段 3：数据库基础发现

- 本地 PostgreSQL 通过 `compose.yaml` 的 `postgres:17-alpine` 服务运行；默认端口为 `5432`，持久卷为 `postgres-data`。
- `infra/postgres/init/001_extensions.sql` 只安装 `pg_trgm`；后续业务表结构将由 Flyway 版本化迁移负责，避免 Docker 首次初始化脚本无法升级既有数据库的问题。
- 数据库设计固定 7 张核心表：`users`、`vehicles`、`resident_profiles`、`long_term_profiles`、`import_batches`、`import_rows`、`audit_logs`。
- Ktor 当前以 `Application.module()` 为唯一入口，测试采用 `testApplication`；数据库迁移必须集成至启动流程并使用独立 PostgreSQL 环境验证。
- 本机 Docker `29.7.1` 的最低 API 版本为 `1.40`，与 Testcontainers `1.20.6` 的默认 API `1.32` 不兼容；当前迁移集成验证采用 Docker Compose 全新卷和 SQL 断言，后续升级测试容器方案前须先验证 Docker API 兼容性。

## 阶段 4：登录与权限发现

- `users` 已具备用户名、密码哈希、角色、状态和版本字段，但没有刷新会话表；会话撤销需通过新的 Flyway V2 迁移实现。
- API 契约当前仅描述登录成功和失败，未定义刷新、退出、令牌字段或错误码；客户端实现前必须补齐契约。
- Android 已有 Compose、Material 3 和 Hilt，但无导航、网络客户端、DataStore、ViewModel 或特性目录；登录功能将按 Android 开发规范从 `feature/auth` 开始建立。
- 审计写入器已可复用，认证审计只记录账号标识、结果、请求标识和操作类型，禁止记录密码、访问令牌或刷新令牌。

## 阶段 5：Excel 结构发现

- 村民车辆源表包含 1 个工作表、382 行和 6 列；首行是标题层，不是字段表头。
- 长期车辆源表包含驻景区单位、驻景区企业、干部车辆和旅游发展公司 4 个工作表；行数、列数和标题层级均不同。
- 两份源表及 Office 临时文件继续由 `.gitignore` 排除；分析只输出工作表和字段结构统计，不记录或展示任何个人信息。
- 村民表字段表头位于第 3 行：序号、姓名、身份证号、车牌号、联系方式、备注。
- 驻景区单位、企业和干部表字段表头位于第 2 行：单位名称、车牌号、车辆类型、通行人员、车辆用途、通行区域和备注；干部表额外包含职务。
- 旅游发展公司表字段表头位于第 2 行：车牌号、品牌型号、核载人数、号牌颜色和备注。

## 阶段 5：Excel 导入实现结论

- 工作簿解析按字段表头辅助识别分类，不依赖 `Sheet1` 等不稳定工作表名称；长期车辆仍使用工作表名称区分单位、企业和干部类别。
- `import_rows` 使用源工作表、源行和拆分序号唯一，单元格中有多个可识别车牌时生成多条待确认记录。
- 车牌为空、无法规范化、首条长期车辆缺少单位、村民姓名或身份证号为空，以及超出数据库字段长度上限均为异常；身份证号和联系方式格式异常为待管理员确认的警告。
- 预览阶段只写入批次和行记录；发布在事务中创建或更新正式资料，更新前保存快照与车辆版本；回滚在版本不一致时整体拒绝，避免影响之后的编辑或其他导入批次。
- 真实 Excel 仅用于隔离 Docker Compose 环境的无输出验证，未提交到 Git、未写入测试夹具，也未在操作日志中记录人员字段。

## 阶段 6：车辆查询实现结论

- 车牌归一化已从导入解析器提取为车辆领域工具，导入和查询均统一为大写并移除无语义分隔符与非车牌字符。
- 最新业务规则固定为归一化后至少 4 个有效车牌字符才查询，字母大小写不影响匹配；候选固定最多 20 条，仅返回车牌和车辆所属类型。
- 查询排序固定为精确、前缀、包含匹配，再按规范化车牌和车辆 ID 稳定排序；查询只读取 `ACTIVE` 状态车辆。
- 车辆详情返回对应村民或长期车辆资料，身份证号和联系方式只存在于详情响应，不进入候选、审计元数据、应用日志或测试输出。
- 详情查询成功与未命中均写入 `audit_logs`，记录操作人、车辆标识、结果、规范化车牌（成功时）和设备类别。

## 阶段 7：Android 普通用户查询实现结论

- 查询页按 `feature/search` 组织，`SearchViewModel` 使用 `StateFlow` 表示输入、候选、历史、加载和失败状态；候选点击通过一次性事件导航到详情页。
- 输入先经 `PlateQueryNormalizer` 统一大写、移除分隔符和无效字符，再以 250 毫秒防抖触发查询；归一化后不足 4 个字符时不调用接口。
- 网络层通过 Retrofit 调用已完成的车辆候选与详情接口，DTO 与领域模型分离；401 响应会清除会话并回到重新登录状态。
- 本机 Room 仅记录账号、车辆标识、车牌、类别和查询时间，按账号隔离并限制为最近 50 条；身份证号、联系方式和详情字段不写入本机历史。
- 语音输入使用 Android 系统 `SpeechRecognizer`。录音权限被拒绝、设备没有识别服务或未识别到内容时，界面保留可编辑的手动输入路径。
- 查询和详情页使用 Material 3、稳定列表键、可读的图标说明和最小点击尺寸；候选只显示车牌与车辆所属类型，详细核验字段仅在详情页展示。

## 阶段 8：管理员维护实现结论

- 管理员 API 使用既有 JWT `requireAdministrator()` 保护，车辆、账号、导入列表与审计列表均由服务端最终判定角色；普通用户无法看到管理员路由，直接请求管理员 API 也会返回 403。
- 车辆档案支持五类车辆的创建、读取、编辑和逻辑停用。车牌经过统一归一化，更新请求必须携带版本号；版本不一致时返回冲突，避免覆盖其他管理员的编辑。
- 账号管理支持创建、角色调整、启用与停用。服务端禁止当前登录管理员停用或降级自身，并拒绝停用最后一个启用管理员。
- 导入中心复用既有预览、行处置、发布和回滚工作流，新增批次摘要列表；Android 使用系统文件选择器读取 Excel，未申请存储权限。
- 审计中心只展示操作者、操作、目标、结果和时间。车辆、账号、导入和审计查询均写入追加式审计记录，不返回密码、令牌或身份资料。
- 根目录 `.gitignore` 已将递归 `data/` 修正为 `/data/`，避免 Android 的 Retrofit、Room 和车辆数据层源码再次被错误忽略。

## 界面方向

- 场景：景区入口或巡查人员快速核验车辆归属与通行身份。
- 色彩：冷杉深绿、冰湖浅色、核验青、通行琥珀、异常红。
- 车牌使用蓝/黄、接近真实比例的领域组件；不以装饰性卡片替代信息层级。
- 结果顺序：车辆归属、核验对象、通行信息、其余字段。
